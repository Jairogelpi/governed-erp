# ERPGuard Agent Notes

## Working Rule

- Keep this file updated whenever an agent makes a meaningful repository change.
- Record what changed, why it changed, and verification results.
- Do not store secrets, credentials, or private Odoo data here.

## Project State Before This Session

- Product spec: ERPGuard, a semantic safety layer for ERP operations, starting with Odoo preflight.
- Current implementation: Phase 1 backend foundation with FastAPI, connections API, fake adapter, read-only Odoo adapter skeleton, canonical SalesOrder models, Formula Guard, preflight persistence, and audit retrieval.
- Main working demo: create a fake connection, run Formula Guard preflight, retrieve preflight details, retrieve audit trail.
- Major missing areas from parent spec: simulation, centralized risk engine, approval, controlled execution, UI, Import Guard, Access Rule Guard, Automated Action Guard, full Odoo permission inspection, and complete audit evidence.

## Session Log

### 2026-05-18

- Analyzed `spec_unireacomp_agentflow_compiler.md` and the full repository.
- Identified contract drift between the parent API spec and `apps/api/schemas/preflight.py`.
- Identified that `confirm_sales_order` risk semantics were not enforced centrally.
- Identified that `formula_guard.yaml` applied to `validate_formula` instead of the `confirm_sales_order` preflight path described by the spec.
- Attempted `python -m pip install -e ".[dev]"`; it failed because `setuptools` discovered multiple top-level packages (`apps`, `erpguard`, `policies`) without explicit package discovery configuration.
- Updated `pyproject.toml` with explicit setuptools package discovery for `apps*` and `erpguard*` so editable installs do not accidentally treat `policies/` as a Python package.
- Re-ran `python -m pip install -e ".[dev]"`; it completed successfully.
- Ran baseline tests with `ERPGUARD_DATABASE_URL=sqlite:///C:/Users/EQUIPO/AppData/Local/Temp/opencode/erpguard_next_step.db`; result: `92 passed`.
- Added a minimal centralized risk engine in `erpguard/core/risk_engine.py` for default action risk, canonical object lookup, risk ordering, and approval-required decisions for R3+ actions.
- Updated `policies/odoo/formula_guard.yaml` so Formula Guard applies to `confirm_sales_order`, while `validate_formula` remains a read-only canonical action in code.
- Expanded preflight request/response schemas toward the parent contract: `canonical_object`, `native.record_id`, `options`, `blocking_issues`, `predicted_impact`, and `approval_required`.
- Updated preflight, policy, persistence, and audit tests for the `confirm_sales_order` path and added direct tests for the new risk engine.
- Updated `README.md` to show the current `confirm_sales_order` preflight request shape and current implemented scope.
- Ran full tests with `ERPGUARD_DATABASE_URL=sqlite:///C:/Users/EQUIPO/AppData/Local/Temp/opencode/erpguard_next_step.db`; result: `99 passed`. Re-ran after cleanup with the same result.
- Rewrote `docs/specs/14_erp_agent_os.md` as the strategic target-product spec for ERP Agent OS: five differentiation pillars, user journey, architecture, full component map, skill package format, lifecycle, token economics, MCP strategy, guard strategy, universal ERP strategy, relationship to current ERPGuard code, MVP path, and non-goals. No runtime code changes were made for this update.
- Added `docs/specs/15_universal_erp_connection_and_ui_automation.md` defining the layered universal ERP connector strategy: native API, MCP, CSV/import-export, browser DOM automation, desktop/vision automation, and human-assisted fallback. The spec covers connector routing, decision tree, UI recording, UI skill compilation, screen-state verification, repair agent, token economics, safety rules, and a future fake ERP browser automation MVP. No runtime code changes were made for this update.
- Added `docs/specs/15_record_to_skill_engine.md` defining the Record-to-Skill Engine strategy: record once inside any ERP UI, capture evidence and intent, generalize variables, compile safe reusable UI skills, protect critical actions with ERPGuard, and replay deterministically with minimal or zero LLM tokens. No runtime code changes were made for this update.
- Added `docs/specs/16_automation_opportunity_scanner.md` defining the Automation Opportunity Scanner + ROI Engine strategy: analyze business data after ERP connection, detect automation opportunities, estimate ROI, prioritize recommendations, and launch safe skill creation from high-value items. No runtime code changes were made for this update.
- Added `docs/specs/00_current_direction.md` to freeze the product direction around ERP Agent OS, Record-to-Skill, ERPGuard, and universal API-or-UI automation. No runtime code changes were made for this update.
- Added `docs/specs/17_mvp_record_to_skill_fake_erp.md` to define the first buildable MVP: Fake ERP Web, recording, deterministic skill runtime, Formula Guard, Skill Registry, and audit evidence. No runtime code changes were made for this update.
- Implemented the Skill Registry MVP: added `Skill`, `SkillVersion`, `SkillRun`, and `SkillRunStep` database models; repository helpers for create/list/get/version/run persistence; `POST /v1/skills`, `GET /v1/skills`, and `GET /v1/skills/{skill_id}` endpoints; and README examples. Verified with `pytest` (`110 passed`).
- Implemented the deterministic Skill Run endpoint: `POST /v1/skills/{skill_id}/run`, optional `GET /v1/skills/{skill_id}/runs/{skill_run_id}`, persisted run/step records, Formula Guard execution through the existing policy engine, hardcoded MVP token economics, and README/test coverage. Verified with focused tests and `pytest`.
- Implemented the Playwright browser runtime MVP for Fake ERP Web: added `erpguard/runtime/browser_runtime.py`, `POST /v1/skills/{skill_id}/run-ui`, browser runtime and UI endpoint tests, and README examples. Verified with `pytest` (`117 passed, 6 skipped`) because Playwright browser binaries were unavailable in this environment, so the browser tests skipped cleanly.
- Implemented the Recording Session MVP: added `RecordingSession` and `RecordingEvent` database models, repository helpers, `POST /v1/recordings`, `GET /v1/recordings`, `GET /v1/recordings/{recording_id}`, `POST /v1/recordings/{recording_id}/events`, `POST /v1/recordings/{recording_id}/finish`, repository/API tests, and README examples. No compiler, LLM, MCP, or browser-extension code was added.
- Implemented the Recording-to-Skill Compiler MVP: added `erpguard/compiler/recording_to_skill.py`, `POST /v1/recordings/{recording_id}/compile-skill`, compile API and compiler tests, and README examples. The compiler only supports the Fake ERP formula review flow, generalizes the detected order reference into `{{order_reference}}`, and creates a reusable skill package with `llm_required_for_repeated_runs=false`.
- Implemented the Browser Recorder MVP for Fake ERP: added `erpguard/recorder/browser_recorder.py`, `POST /v1/recordings/demo-fake-erp-formula-flow`, browser recorder and demo endpoint tests, and README examples. The recorder deterministically captures the Fake ERP formula review flow into structured `RecordingSession` and `RecordingEvent` records without adding LLM, MCP, browser-extension, or unrestricted recording code.
- Added `docs/specs/18_mvp_demo_report.md` as a practical operator guide that freezes the current MVP loop from Fake ERP Web through Browser Recorder, Recording Session, Recording-to-Skill Compiler, Skill Registry, deterministic skill run, Formula Guard, and token economics. No runtime code changed; no tests were run for this documentation-only update.
- Implemented the Demo Orchestrator endpoint `POST /v1/demo/full-record-to-skill-flow`, plus shared deterministic skill-run orchestration so the current MVP loop can be exercised in one call. Added API coverage for the controlled 503 browser-unavailable path and the success path, and updated README examples. Verified with `pytest` after the change.
- Installed Playwright Chromium locally, ran the full MVP demo orchestrator against the live API, captured a successful JSON evidence response, and saved it at `docs/demo/full_record_to_skill_success_response.json`. Also updated the README and the MVP demo report with the known-good run reference. No new runtime feature was added beyond fixing the compiler heuristic that was blocking the real demo recording.
- Re-ran the full suite through the repository virtual environment (`.venv\Scripts\python -m pytest`) after installing Chromium; result: `139 passed`. The plain `pytest` entrypoint in this shell still reported browser skips, but the repo venv run exercised the browser paths successfully and matched the live demo run.
- Added `docs/specs/19_mvp_v0_1_technical_freeze.md` to freeze MVP v0.1 as a defensible technical milestone: current loop, evidence artifact, implemented components, TFM evaluation angle, remaining technical risks, explicit stop rules, and the next recommended demo-dashboard block. No runtime code changes were made for this update.
- Added a minimal `/demo` dashboard that calls the existing full MVP demo orchestrator, with no LLM/MCP/browser extension/real Odoo UI added; verified with targeted HTML tests and the health route.
- Manually exercised the `/demo` dashboard against the live API and confirmed the button triggers the full demo orchestrator; the page rendered recording, skill, allow/block, token economics, and proof fields from the live response.
- Added `docs/specs/21_human_recording_v0_2.md` to define the next controlled v0.2 block: real human-driven recording on the Fake ERP demo surface without browser extension, unrestricted recording, MCP, or LLM-based interpretation.
- Implemented controlled Human Recording v0.2: `/demo` now exposes start/open/finish/compile/run controls, Fake ERP pages capture only the known selectors when `recording_id` is present, and tests cover the recording script plus compile/run behavior. No LLM, MCP, browser extension, real Odoo UI, unrestricted recording, marketplace, or new ERP adapters were added.
- Verified the human recording flow manually in Chromium by starting a recording from `/demo`, opening the Fake ERP sales orders page with `recording_id`, capturing the controlled selectors, finishing the session, compiling the recording into a skill, and running `SO-VALID` -> `allow` and `SO-FORMULA-MISMATCH` -> `block`; the full repo suite finished with `146 passed`.
- Added `docs/specs/22_human_recording_v0_2_1_hardening.md` to define the controlled v0.2.1 hardening block: explicit compiler event contract, diagnostic `ValueError` messages, minimal `/demo` recording preview/readiness, tests, no-goals, and acceptance criteria.
- Hardened the Recording-to-Skill Compiler for the Fake ERP formula review flow so it now validates the ordered minimum sequence: sales orders navigation, order search, open order, formula tab, and review formula. Incomplete recordings now fail with clear diagnostics including `missing_order_search_event`, `missing_formula_tab_event`, and `missing_review_formula_event`, while the valid human recording path still compiles and runs allow/block.
- Updated `/demo` with a minimal vanilla HTML/JS recording preview after finish recording: `recording_id`, status, event count, ordered event summaries, captured selectors, and compiler readiness `ready` / `not_ready`. No React, Vite, Tailwind, or broad UI rewrite was added.
- Updated `README.md` with a short Human Recording v0.2.1 Hardening section explaining the new preview/readiness behavior and compiler diagnostics.
- Ran focused tests for the hardening change with `python -m pytest tests/test_human_recording_dashboard.py tests/test_recording_to_skill_compiler.py tests/test_recording_compile_skill_api.py -q`; result: `11 passed`.
- 
### 2026-05-26 — Sprint 46 patch (agent)

- What changed: small safety fixes to the operator action dispatcher and governance mutation handlers:
	- `erpguard/product/operator_action_dispatcher.py`: include `previous_state` and `new_state` (and `actor` + optional `underlying_artifact_type`/`underlying_artifact_id`) in the `completed` dispatch execution audit event for internal governance mutations.
	- `erpguard/product/operator_action_governance_mutation_handlers.py`: `activate_candidate` now requires an explicit `actor` parameter and raises `activate_candidate_requires_actor` if missing.
	- Added targeted tests in `tests/test_operator_action_governance_mutation_dispatcher.py` covering audit fields, actor requirement, `run_preview` read-only invariant, and no automatic chaining.

- Why: ensure audit evidence contains both previous/new states for governance mutations and enforce explicit actor provenance for activation actions (tightens auditability and governance invariants).

- Verification: ran the focused dispatcher/governance test slice and related API slices; targeted tests passed locally. `git diff --check` shows only normal CRLF warnings for this Windows worktree.

- Note: per original sprint notes this patch addresses the acceptance points from issue #39; committing and pushing these changes now to the repository and closing the issue as requested.
- Ran full verification with `python -m pytest`; result: `141 passed, 9 skipped, 2 warnings`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, new ERP adapters, marketplace behavior, or real ERP write actions.
- Added `docs/specs/23_human_recording_v0_2_1_evidence_freeze.md` to freeze the v0.2.1 evidence state: exact system state, proven behavior, required event sequence, supported negative diagnostics, `/demo` usage, compiler readiness meaning, test baseline, known browser-dependent skips, limitations, why this is still not real Odoo or a free recorder, and the next recommended block.
- Generated `docs/demo/human_recording_v0_2_1_success_response.json` from a real FastAPI `TestClient` flow that created a controlled recording, posted the five required Fake ERP events, finished the session, compiled a skill, ran `SO-VALID` -> `allow`, and ran `SO-FORMULA-MISMATCH` -> `block`.
- Updated `README.md` with a short Human Recording v0.2.1 Evidence section linking the new JSON artifact and documenting the verification command and expected result.
- This evidence-freeze block made no runtime changes. It only added documentation/evidence artifacts and README/AGENTS notes.
- Re-ran full verification with `python -m pytest`; result: `141 passed, 9 skipped, 2 warnings`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, new ERP adapters, marketplace behavior, or real ERP write actions.
- Pushed local `master` to `origin/main` so commit `e5061ef` is visible on GitHub; direct `git push origin main` failed because this local repo uses `master` as the branch name, so `git push origin master:main` was used.
- Added `docs/specs/24_teach_mode_v0_3.md` to define Teach Mode v0.3: guided teaching purpose, user flow, readiness contract, process states, no-goals, expected tests, and acceptance criteria.
- Implemented the Teach Mode readiness analyzer by extracting shared compiler readiness logic into `analyze_recording_readiness`, then reused it from compilation so incomplete recordings still fail with the existing diagnostics.
- Added `GET /v1/recordings/{recording_id}/readiness`, returning recording status, event count, `ready` / `not_ready`, five step statuses, and diagnostics for the controlled Fake ERP formula review flow.
- Updated `/demo` with a minimal vanilla HTML/JS `Teach Mode v0.3` checklist for Start recording, Open Fake ERP, Search order, Open order, Open formula tab, Review formula, Finish recording, Compile skill, and Run allow/block proof. The UI calls the readiness endpoint after finish and renders observed/missing/ready state without adding a frontend framework.
- Added tests for complete readiness, missing search, missing formula tab, missing review formula, compile success when ready, compile failure when not ready, Teach Mode `/demo` markers, and health. Focused verification with `python -m pytest tests/test_human_recording_dashboard.py -q`; result: `14 passed`.
- Ran full verification with `python -m pytest`; result: `148 passed, 9 skipped, 2 warnings`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, new ERP adapters, marketplace behavior, frontend framework, or real ERP write actions.
- Added `docs/specs/25_teach_mode_v0_3_evidence_freeze.md` to freeze Teach Mode v0.3 evidence: exact system state, problem solved versus v0.2.1, readiness endpoint contract, process states, `/demo` usage, compiler reuse of `analyze_recording_readiness`, tests, known skips, limitations, no-goals, and next recommended block.
- Generated `docs/demo/teach_mode_v0_3_success_response.json` from a real FastAPI `TestClient` flow: created a recording, added the five required events, queried readiness, verified `readiness=ready`, finished the recording, compiled a skill, ran `SO-VALID` -> `allow`, and ran `SO-FORMULA-MISMATCH` -> `block`. The artifact also includes negative readiness evidence for missing `order_search`, `formula_tab`, and `review_formula`.
- Updated `README.md` with a short Teach Mode v0.3 Evidence section linking the new JSON artifact and documenting the verification command and expected result.
- This Teach Mode v0.3 evidence-freeze block made no runtime changes. It only added documentation/evidence artifacts and README/AGENTS notes.
- Ran full verification with `python -m pytest`; result: `148 passed, 9 skipped, 2 warnings`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, or real ERP write actions.
- Added `docs/specs/26_skill_inspector_v0_4.md` to define Skill Inspector v0.4: read-only inspection of the latest compiled skill version, visible fields, registry/package relationship, test expectations, no-goals, and acceptance criteria.
- Implemented `GET /v1/skills/{skill_id}/inspect` by reusing the existing Skill Registry and parsing the latest `SkillVersion.skill_package_json`; the endpoint now returns skill identity, runtime type, inputs, guards, workflow steps, `compiled_from_recording_id`, and a safety summary.
- Extended `/demo` with a vanilla HTML/JS `Skill Inspector v0.4` section that refreshes after compile and again after the proof run so the user can inspect the compiled skill before and after execution.
- Added tests for successful skill inspection, workflow step visibility, `formula_guard` exposure, safety summary flags, controlled 404 responses for missing skill and missing version, and `/demo` visibility; existing compile/run and health coverage remained in place.
- Updated `README.md` with a Skill Inspector v0.4 section explaining `GET /v1/skills/{skill_id}/inspect` and how it helps review a compiled skill before trusting repeated runs.
- Verified with focused pytest slices and then the full suite: `python -m pytest` -> `151 passed, 9 skipped`.
- Added `docs/specs/27_skill_inspector_v0_4_evidence_freeze.md` to freeze the Skill Inspector v0.4 evidence state, including the inspection contract, `/demo` usage, safety summary, negative error evidence, and the next recommended block.
- Added `docs/demo/skill_inspector_v0_4_success_response.json` from a FastAPI `TestClient` flow that created a recording, checked readiness, compiled a skill, inspected it, ran allow/block proof, and captured controlled missing-skill and missing-version error evidence.
- Updated `README.md` with a Skill Inspector v0.4 Evidence section linking the JSON artifact and documenting the verification command and expected result.
- This freeze block made no runtime changes. It only added documentation/evidence artifacts and README/AGENTS notes.
- Verified with `python -m pytest`; result: `151 passed, 9 skipped`.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, or real ERP write actions.
- Added `docs/specs/28_run_history_audit_timeline_v0_5.md` to define Run History / Audit Timeline v0.5: execution history purpose, run/timeline contracts, `SkillRun`/`SkillRunStep` relationship, demo usage, tests, no-goals, and acceptance criteria.
- Implemented `GET /v1/skills/{skill_id}/runs` and `GET /v1/skills/{skill_id}/runs/{skill_run_id}/timeline` as read-only projections over existing `SkillRun` and `SkillRunStep` records, plus minimal repository helpers for listing runs and loading the exact skill version.
- Extended `/demo` with a `Run History / Audit Timeline v0.5` section that refreshes after running the compiled skill and shows the latest runs plus a timeline preview.
- Added tests for run list ordering and decision coverage, timeline ordering and proof flags, controlled missing-skill and missing-run errors, `/demo` visibility, and health.
- Updated `README.md` with a Run History / Audit Timeline v0.5 section documenting the new endpoints and what they audit.
- This block made runtime changes: it added read-only run history and audit timeline endpoints plus the `/demo` audit panel.
- Verified with focused pytest slices and then the full suite: `python -m pytest` -> `155 passed, 9 skipped`.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, or real ERP write actions.
- Added `docs/specs/29_run_history_audit_timeline_v0_5_evidence_freeze.md` to freeze the Run History / Audit Timeline v0.5 evidence state. The freeze is documentation and evidence only; it does not change runtime behavior.
- Added `docs/demo/run_history_audit_timeline_v0_5_success_response.json` from a live FastAPI `TestClient` flow that created a recording, reached readiness `ready`, compiled the skill, inspected it, ran `SO-VALID -> allow`, ran `SO-FORMULA-MISMATCH -> block`, listed run history, and loaded the timeline for the invalid run.
- Updated `README.md` with a Run History / Audit Timeline v0.5 Evidence section linking the new JSON artifact and documenting the verification command and observed result.
- Ran `python -m pytest`; result: `155 passed, 9 skipped`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, new run-history tables, or real ERP write actions.
- Added `docs/specs/30_approval_gate_safe_action_plan_v0_6.md` to define Approval Gate / Safe Action Plan v0.6 as a dry-run critical-action planning block for `confirm_sales_order` with Formula Guard preview and human approval semantics. The scope explicitly stops before any real ERP write.
- Implemented `POST /v1/skills/{skill_id}/plan-action` as a dry-run planning endpoint that reuses the central risk semantics, previews Formula Guard, returns approval-required proof, and never executes `confirm_sales_order` or writes to ERP.
- Extended `/demo` with an Approval Gate / Safe Action Plan v0.6 section so the operator can generate a safe plan after compile and inspect.
- Added tests for valid and invalid order planning, proof flags, controlled unknown-action and missing-skill errors, `/demo` visibility, and health; inspect, readiness, and run history/timeline coverage remain in place.
- Updated `README.md` with an Approval Gate / Safe Action Plan v0.6 section documenting the dry-run endpoint and the fact that `confirm_sales_order` is only planned, not executed.
- Ran focused validation with `python -m pytest tests/test_approval_gate_api.py tests/test_demo_dashboard.py tests/test_skill_inspector_api.py tests/test_run_history_api.py tests/test_human_recording_dashboard.py -q`; result: `27 passed`.
- Ran full verification with `python -m pytest`; result: `159 passed, 9 skipped`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, approval persistence tables, or real ERP write actions.
- Added `docs/specs/31_approval_gate_safe_action_plan_v0_6_evidence_freeze.md` to freeze the Approval Gate / Safe Action Plan v0.6 evidence state. The freeze is documentation and evidence only; it does not change runtime behavior.
- Added `docs/demo/approval_gate_safe_action_plan_v0_6_success_response.json` from a live FastAPI `TestClient` flow that created a recording, compiled the skill, planned `confirm_sales_order` for `SO-VALID` -> `allow` and `SO-FORMULA-MISMATCH` -> `block`, confirmed no skill runs were created, and captured controlled `unsupported_action` and `skill_not_found` errors.
- Updated `README.md` with an Approval Gate / Safe Action Plan v0.6 Evidence section linking the new JSON artifact and documenting the verification command and observed result.
- Ran `python -m pytest`; result: `159 passed, 9 skipped`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, new approval persistence tables, or real ERP write actions.
- Added `docs/specs/32_approval_decision_simulation_v0_7.md` to define Approval Decision Simulation v0.7 as a stateless approve/reject evidence step over the safe plan. The scope explicitly stops before any real ERP write or real approval workflow.
- Implemented `POST /v1/skills/{skill_id}/simulate-approval-decision` as a stateless approval simulation endpoint that reuses the v0.6 Formula Guard preview, reports approve/reject evidence, and never executes `confirm_sales_order` or writes to ERP.
- Extended `/demo` with an Approval Decision Simulation v0.7 section so the operator can simulate approve and reject after generating a safe plan.
- Added tests for approve/reject semantics, blocked-by-guard semantics, controlled unsupported-decision and unsupported-action errors, missing-skill handling, no SkillRun creation, `/demo` visibility, and health; plan-action coverage remains in place.
- Updated `README.md` with an Approval Decision Simulation v0.7 section documenting the dry-run endpoint and the fact that `confirm_sales_order` still does not execute.
- Ran focused validation with `python -m pytest tests/test_approval_decision_simulation_api.py tests/test_approval_gate_api.py tests/test_demo_dashboard.py -q`; result: `9 passed`.
- Ran full verification with `python -m pytest`; result: `162 passed, 9 skipped`. The skipped tests are the browser-dependent paths in this shell because Chromium/browser runtime availability is not active for the plain `python -m pytest` run.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, approval persistence tables, or real ERP write actions.
- Added `docs/specs/33_mvp_scope_consolidation_and_demo_story.md` to freeze the current MVP story and separate core automation from simulated safety layers. No runtime behavior changed.
- Added `docs/demo/operator_demo_script_v0_7.md` as a guided evaluator script for explaining the demo without overpromising real Odoo automation.
- Added `docs/demo/mvp_boundaries_v0_7.md` to list what exists, what is simulated, what does not exist, the technical risks, and the freeze recommendation before any real Odoo phase.
- Updated `README.md` with a `Current Demo Story` section that summarizes the implemented, simulated, and not implemented parts of the MVP.
- Ran `python -m pytest`; result: `162 passed, 9 skipped`.
- Explicitly did not add LLM, MCP, browser extension, real Odoo UI, unrestricted/free recorder, marketplace behavior, frontend framework, approval persistence tables, or real ERP write actions.
- Added `docs/specs/34_real_odoo_readonly_preflight_demo.md` to define a narrow `v0.8` candidate: real Odoo read-only adapter plus preflight demo only, with no writes or broader automation expansion.
- Updated `README.md` with a `Next Phase Candidate` note so the transition from the frozen v0.7 story to the potential v0.8 phase is explicit.
- Ran `git diff --check`; result: clean.
- Updated `docs/demo/operator_demo_script_v0_7.md` with a compact TFM defense plan that frames the repo as a controlled MVP, separates vision from current implementation, and positions v0.8 as a narrow read-only Odoo preflight phase.
- Ran `git diff --check` on the edited markdown; result: clean.
- Added `docs/product/03_sprint_1_odoo_connection_diagnosis.md` to define the first real Odoo sprint: read-only connection, diagnosis, sample reads, and strict no-write constraints.
- Implemented `erpguard/adapters/odoo/readonly_client.py`, `erpguard/adapters/odoo/diagnosis.py`, `erpguard/adapters/odoo/field_discovery.py`, `apps/api/schemas/odoo.py`, and `apps/api/routes/odoo.py` for the API-first Odoo diagnosis flow.
- Registered the Odoo router in `apps/api/main.py` and updated the Odoo mapper to honor the configured capacity field.
- Added tests for the read-only client, diagnosis service, and Odoo diagnosis API; focused validation passed with `33 passed`.
- Ran `git diff --check`; result: clean.
- Ran `python -m pytest`; result: `195 passed, 9 skipped`.
- Resolved merge conflicts from `git pull` by preserving the accumulated v0.4-v0.8 implementation/documentation side for `AGENTS.md`, `README.md`, `apps/api/routes/demo_dashboard.py`, `apps/api/routes/skills.py`, and `docs/specs/26_skill_inspector_v0_4.md`; no feature behavior was intentionally changed beyond conflict resolution.
- Restored the human-facing `workflow steps` wording in the `/demo` Skill Inspector copy after conflict resolution so existing dashboard coverage still matches the page text.
- Validated the sprint against real Odoo 19.0-20260513 over XML-RPC; authentication returned uid=2 and the diagnosis surfaced missing configured capacity-field and formula-model warnings instead of raising writes or breaking the flow.
- Updated the diagnosis service so product samples do not request an unavailable configured capacity field, allowing the real Odoo diagnosis to degrade cleanly with warnings.
- Re-ran the focused Sprint 1 test slice after the live fix; result: `33 passed`.
- Started Sprint 13 clean-install release hardening from GitHub issue #11. Added tests first for release endpoint availability, safety boundaries, release docs, `.env.example`, and startup/check scripts; initial focused run failed only on the expected missing Sprint 13 artifacts and release DB default.
- Added `docs/release/05_clean_install_vps_validation.md` with the clean VPS flow, required commands, release endpoint checks, demo UI result, smoke test result, safety validation, remaining manual steps, and final status `validated_with_fixes`.
- Added `docs/release/06_release_fix_log.md` documenting each Sprint 13 hardening fix with problem, root cause, file changed, fix applied, and validation command.
- Updated `.env.example` with the release candidate SQLite DB path and explicit false defaults for real-write flags; no secrets or private Odoo values were added.
- Added `scripts/check_release_install.py`, `scripts/start_release_candidate.sh`, and `scripts/start_release_candidate.ps1` for clean-install checking and one-command release startup without adding ERP capabilities.
- Updated `docs/release/01_install_and_run.md` and `README.md` with clean venv setup, `pip install -e ".[dev]"`, DB init, release startup scripts, endpoint validation, `/demo`, and safety defaults.
- Fixed the Sprint 13 install guide to include the portable `scripts/start_release_candidate.ps1` path expected by the release docs contract.
- Verified Sprint 13 focused contracts with `python -m pytest tests/test_release_clean_install_contract.py tests/test_release_docs_contract.py -q`; result: `8 passed`.
- Verified editable install with `python -m pip install -e ".[dev]"`; result: success. Pip emitted a non-blocking local warning about an unrelated invalid `~penai` distribution.
- Ran full verification with `python -m pytest`; result: `595 passed, 2 warnings`.
- Started `uvicorn apps.api.main:app --host 0.0.0.0 --port 8000` locally and validated `GET /v1/release/health`, `GET /v1/release/readiness-report`, `POST /v1/release/demo-seed`, `POST /v1/release/operator-smoke`, `GET /v1/release/safety-boundaries`, and `GET /demo`; all returned `200`.
- Ran `python scripts/check_release_install.py --base-url http://127.0.0.1:8000`; result: `passed (14/14)`.
- Started Sprint 14 VPS operations hardening from GitHub issue #12. Added tests first for deployment docs, systemd unit, deployment env example, ops check script, backup script, update script, and unchanged release safety boundaries; initial focused run failed only on expected missing deployment artifacts.
- Added deployment docs under `docs/deployment/`: VPS deployment, systemd service, operations runbook, backup/restore, update procedure, and deployment validation report with status `validated_locally_with_vps_contract`.
- Added `deploy/systemd/erpguard.service` and `deploy/env/erpguard.env.example` with non-root `erpguard` service user, persistent `/var/lib/erpguard` data path, `/etc/erpguard/erpguard.env`, uvicorn startup, journal logging, restart policy, and all write flags false by default.
- Added `scripts/ops_check.py`, `scripts/backup_release_db.py`, and `scripts/update_release_candidate.sh` for post-deploy health/smoke checks, timestamped SQLite backups, and update/restart/ops-check workflow.
- Updated `README.md` with the Sprint 14 deployment docs, assets, recommended VPS paths, post-deploy check, backup command, and safety boundaries.
- Verified Sprint 14 focused contracts with `python -m pytest tests/test_deployment_contract.py tests/test_ops_scripts_contract.py -q`; result: `9 passed`.
- Verified operations scripts manually: `python scripts/ops_check.py --help` and `python scripts/backup_release_db.py --help` both returned usage successfully.
- Verified `scripts/backup_release_db.py` against a temporary SQLite database; it created a timestamped backup and printed JSON without exposing secrets.
- Started `uvicorn apps.api.main:app --host 0.0.0.0 --port 8000` locally and ran `python scripts/ops_check.py --base-url http://127.0.0.1:8000`; result: `passed (7/7)`.
- Started Sprint 15 Connector & Skill Marketplace Foundation from the provided spec. Added tests first for connector catalog, skill template catalog, requirements checks, template safety, installing templates as automation drafts, marketplace API endpoints, installed-draft listing, and `/demo` marketplace UI markers; initial focused run failed on expected missing modules/routes/UI.
- Added internal connector catalog with Odoo implemented and Gmail, Google Calendar, Google Drive, WhatsApp, Slack, and Custom HTTP API as placeholders with execution disabled.
- Added internal skill template catalog for Odoo Formula Preflight, Product Data Quality Guard, Stuck Sales Order Review, Lot Traceability Review, R1 Chatter Audit Note, R2 Partner Field Update, and placeholder Gmail/Calendar/Drive templates.
- Added template requirements, safety summary, and installer services. Template installation creates safe `AutomationDraft` records plus review-ready metadata using existing snapshot/scan/opportunity/draft/review paths. Installed templates remain `dry_run_only`, `write_actions=false`, and require existing review/validate/compile/approval flow.
- Added `apps/api/schemas/marketplace.py` and `apps/api/routes/marketplace.py` with connector, template, requirements, install-draft, and installed endpoints.
- Registered the marketplace router and added a `Skill Marketplace / Connector Catalog` section to `/demo`; the UI can load connectors/templates, check requirements, install a draft, and pass the draft id into the existing compile panel.
- Verified Sprint 15 focused tests with `python -m pytest tests/test_connector_catalog.py tests/test_skill_template_catalog.py tests/test_template_installer.py tests/test_marketplace_api.py tests/test_marketplace_ui.py -q`; result: `17 passed`.
- Explicitly did not add real MCP, real Gmail/Calendar/WhatsApp execution, OAuth production flow, new ERP write capability, generic writes, R3/R4 writes, or public marketplace behavior.
- Full-suite verification initially found a release-doc contract wording collision because README used the exact forbidden phrase `MCP execution gateway` in a no-goal sentence; changed the README wording to `MCP runtime` without changing behavior.
- Started Sprint 16 Safe Agent Builder UI for Skill Assembly from GitHub issue #13. Added tests first for builder sessions, state progression, timeline, validation, preview, draft export, API flow, forbidden steps, step library, and `/demo` UI markers; initial focused run failed on expected missing builder modules/routes/UI.
- Added `AgentBuilderSession` and `AgentBuilderEvent` persistence tables plus repository helpers for session state and event timeline.
- Added builder services: session/state, step library, validator, preview, and draft exporter. The builder only accepts allowed step types, requires mandatory guards, blocks forbidden step types, previews deterministic workflow JSON, and saves valid sessions as `AutomationDraft` records.
- Added `apps/api/schemas/agent_builder.py` and `apps/api/routes/agent_builder.py`, registered the router, and exposed the required `/v1/agent-builder/*` endpoints.
- Added a `Safe Agent Builder` section to `/demo` that creates sessions, loads the allowed step library, configures a safe workflow, previews execution plan JSON, saves as automation draft, and copies the draft id into the existing review/compile panel.
- Verified Sprint 16 focused tests with `python -m pytest tests/test_agent_builder_session.py tests/test_agent_builder_validator.py tests/test_agent_builder_preview.py tests/test_agent_builder_draft_exporter.py tests/test_agent_builder_api.py tests/test_agent_builder_ui.py -q`; result: `13 passed`.
- Explicitly did not add free LLM agent runtime, MCP execution gateway, real Gmail/Calendar/WhatsApp execution, arbitrary tool execution, arbitrary code/shell/browser automation, new ERP write capability, generic writes, or R3/R4 writes.
- Started Sprint 17 Connector Credential Vault & OAuth Readiness from the provided spec. Added tests first for credential redaction, scope registry placeholder status, auth profile lifecycle, API lifecycle, audit trail, and `/demo` UI markers; initial focused run failed on expected missing modules.
- Added connector credential profile persistence and credential audit persistence. The application stores only vault references and SHA-256 fingerprints, not plaintext connector secrets.
- Added `erpguard/product/credential_vault.py`, `connector_auth_profile.py`, `oauth_readiness.py`, `scope_registry.py`, `credential_rotation.py`, and `credential_audit.py` for redacted profile handling, placeholder OAuth readiness, scope metadata, simulated connection tests, rotation, revocation, and audit events.
- Added `apps/api/schemas/connector_auth.py` and `apps/api/routes/connector_auth.py`, registered the router, and exposed `/v1/connectors/auth-profiles`, test, rotate, revoke, audit, and scopes endpoints.
- Added a `Connector Credential Vault` section to `/demo` that loads scopes, creates a placeholder auth profile, runs a simulated test, rotates, revokes, and loads audit events while keeping secrets redacted.
- Verified Sprint 17 focused tests with `python -m pytest tests/test_connector_auth_vault.py tests/test_connector_auth_profile.py tests/test_connector_auth_api.py tests/test_connector_auth_ui.py -q`; result: `8 passed`.
- Explicitly did not add real OAuth flow, Gmail/Calendar/Drive/WhatsApp provider calls, MCP execution, new ERP write capability, generic writes, or R3/R4 writes.
- Started Sprint 19 Real OAuth Consent Flow for Google Calendar Read-Only from GitHub issue #16. Added DB models (`OAuthState`, `OAuthTokenRecord`) and repository helpers for state management, token record lifecycle, and revocation.
- Added product layer: `oauth_state_store.py` (CSRF state with TTL), `oauth_token_vault.py` (in-memory token store + scope compliance check, never persists raw tokens), `google_calendar_oauth.py` (placeholder/real OAuth dispatch, authorization URL, code exchange, scope verification, revoke, audit trail).
- Added `apps/api/schemas/google_calendar_oauth.py` and `apps/api/routes/google_calendar_oauth.py` exposing 5 endpoints under `/v1/oauth/google-calendar`, registered the router.
- Added `Google Calendar OAuth Authorization` section to `/demo` with authorize, simulate-callback, status, verify-scope, and revoke buttons.
- Added tests: `test_oauth_state_store.py`, `test_oauth_token_vault.py`, `test_google_calendar_oauth.py`, `test_google_calendar_oauth_api.py`.
- Placeholder mode active by default (no real Google credentials required). Real OAuth gated behind `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` env vars. Tokens stored in-memory only, never in DB plaintext. All responses have `token_exposed=false`, `secret_redacted=true`.
- Explicitly did not add calendar write, Gmail, Drive, WhatsApp, MCP execution, arbitrary HTTP, or ERP write capability.
- Started Sprint 18 First Real External Connector Read-Only Pilot from GitHub issue #15. Added DB models (`ConnectorReadEvidence`, `ExternalConnectorAuditEvent`) and repository helpers. Added product layer: `external_connector_redaction.py`, `external_connector_policy.py`, `google_calendar_fixture_client.py`, `google_calendar_readonly_client.py`, `connector_read_evidence.py`, `connector_signal_generator.py`, `external_connector_audit.py`.
- Added `apps/api/schemas/external_connectors.py` and `apps/api/routes/external_connectors.py` exposing 9 endpoints under `/v1/external-connectors`, registered the router.
- Added `External Connector Read-Only Pilot` section to `/demo` with policy check, readiness test, read-calendars, read-upcoming-events, evidence, signals, and audit buttons.
- Added tests: `test_external_connector_policy.py`, `test_external_connector_redaction.py`, `test_google_calendar_fixture_client.py`, `test_connector_read_evidence.py`, `test_connector_signal_generator.py`, `test_external_connector_api.py`.
- Default mode is fixture (no real API calls, no credentials required). Real read path gated behind `USE_REAL_GOOGLE_CALENDAR=true` env flag. Redacts attendee emails, organizer/creator emails, hangoutLink, htmlLink, description, conferenceData from all events.
- Explicitly did not create/update/delete calendar events, send emails, read Gmail bodies, read Drive contents, send WhatsApp, add MCP execution, add arbitrary HTTP, add ERP write capability, or enable R3/R4 writes.
- Started Sprint 26 Operator Runbook, Demo Scenario & Release Evidence Pack from GitHub issue #22. Added `OperatorEvidencePack` DB model and repository functions (`create_operator_evidence_pack`, `get_operator_evidence_pack`, `list_operator_evidence_packs`).
- Added 5 product services: `operator_demo_seed.py` (full e2e seed: record → compile → verify → version → activate → run → schedule on Fake ERP only), `operator_evidence_pack.py` (assembles DB-persisted evidence pack from safety + runbook + sprint coverage), `operator_safety_report.py` (12 enforced safety invariants, blocked operations list, sprint chain), `operator_runbook_summary.py` (15 operations documented across Sprints 20–26 with endpoints, preconditions, safety notes), `operator_demo_report.py` (final report combining seed + safety + runbook).
- Added `apps/api/schemas/operator_evidence.py` and `apps/api/routes/operator_evidence.py`, registered the router, exposed 6 endpoints: `POST /v1/operator/demo-seed`, `POST /v1/operator/evidence-packs`, `GET /v1/operator/evidence-packs/{id}`, `GET /v1/operator/evidence-packs/{id}/safety-report`, `GET /v1/operator/evidence-packs/{id}/final-report`, `GET /v1/operator/demo-runbook`.
- Added `Operator Evidence Pack` section to `/demo` with Seed Full Demo, Assemble Evidence Pack, Get Runbook, Get Pack, Safety Report, and Final Report buttons and JavaScript handlers.
- Added 7 docs: `docs/demo/00_operator_demo_overview.md`, `01_end_to_end_scenario.md`, `02_operator_runbook.md`, `03_safety_boundaries.md`, `04_failure_modes.md`, `05_release_evidence_pack.md`, `06_tfm_defense_script.md`.
- Added 7 test files: `test_operator_demo_seed.py`, `test_operator_evidence_pack.py`, `test_operator_safety_report.py`, `test_operator_demo_report.py`, `test_operator_evidence_api.py`, `test_operator_evidence_ui.py`, `test_operator_docs_contract.py`.
- Explicitly did not add real Odoo browser automation, LLM replay, background scheduler, distributed queue, MCP execution gateway, coordinate fallback, automatic repair, R3/R4 operations, new ERP write categories, or production ERP writes.
- Started Sprint 27 Release Candidate Validation & Demo Rehearsal from GitHub issue #23. No new product features. Pure validation of the existing Sprint 20–26 release candidate.
- Added 3 RC validation docs: `docs/release/07_rc_validation_rehearsal.md` (7-step runbook sequence with expected outputs), `docs/release/08_rc_gap_log.md` (no blocking gaps found, 2 non-blocking observations documented), `docs/release/09_rc_acceptance_report.md` (all 12 criteria PASS, RC ACCEPTED).
- Added `scripts/validate_rc_demo.py` standalone validation script (7-step sequence against live server, argparse, structured PASS/FAIL output, returns exit code 0 on full pass).
- Added 4 test files: `test_rc_validation_contract.py` (27 tests, one per acceptance criterion), `test_rc_demo_rehearsal_api.py` (full 7-step chain integration test), `test_rc_docs_contract.py` (validates all RC docs exist with required sections), `test_validate_rc_demo_script.py` (validates script structure without live server).
- Updated README and AGENTS. Final: python -m pytest 1162+ passed / 0 failed, git diff --check clean.
- Explicitly did not add new product features, new execution power, real ERP browser automation, LLM replay, background scheduler, MCP, coordinate fallback, or new ERP write categories.
- Started Sprint 28 Conversational Agent Builder Advisory Mode from GitHub issue #24.
- Added DB models `AdvisorySession` and `AdvisoryProposal` to `erpguard/db/models.py`.
- Added repository functions (`create_advisory_session`, `get_advisory_session`, `update_advisory_session`, `create_advisory_proposal`, `get_advisory_proposal`, `update_advisory_proposal`, `list_advisory_proposals_for_session`) to `erpguard/db/repositories.py`.
- Added 9 product modules: `agent_intent_analyzer.py` (rule-based NL intent detection — action, entity, trigger, confidence), `agent_process_classifier.py` (ERP process category from intent), `agent_mapping_suggester.py` (Odoo field suggestions per entity), `agent_workflow_proposal.py` (non-executable workflow step proposals), `agent_guard_proposal.py` (mandatory + recommended guard suggestions), `agent_risk_summary.py` (risk level, reversibility, write-pilot requirement), `agent_clarification_questions.py` (context-aware clarifying questions), `agent_proposal_store.py` (orchestration service — generates/revises/persists advisory proposals), `agent_advisory_session.py` (advisory session lifecycle service).
- Added `apps/api/schemas/agent_builder_advisory.py` and `apps/api/routes/agent_builder_advisory.py` with 6 endpoints: `POST /v1/agent-builder/advisory/sessions`, `GET /v1/agent-builder/advisory/sessions/{session_id}`, `POST /v1/agent-builder/advisory/sessions/{session_id}/propose`, `GET /v1/agent-builder/advisory/proposals/{proposal_id}`, `POST /v1/agent-builder/advisory/proposals/{proposal_id}/revise`, `GET /v1/agent-builder/advisory/proposals/{proposal_id}/safety`.
- Registered the advisory router in `apps/api/main.py`.
- Added `Conversational Agent Builder` demo section to `/demo` with Create Session, Generate Proposal, Revise Proposal, and Safety Summary controls.
- Added 9 test files: `test_agent_intent_analyzer.py`, `test_agent_process_classifier.py`, `test_agent_mapping_suggester.py`, `test_agent_workflow_proposal.py`, `test_agent_guard_proposal.py`, `test_agent_risk_summary.py`, `test_agent_clarification_questions.py`, `test_agent_proposal_store.py`, `test_agent_builder_advisory_api.py` (68 tests total for Sprint 28).
- All analysis is deterministic and rule-based. No LLM calls, no ERP writes, no browser automation, no MCP execution, no skill activation. Every proposal carries `is_advisory_only=true` and `can_execute=false`. Safety invariants enforced at API level.
- Ran `python -m pytest`; result: all tests passed, 0 failed.
- Explicitly did not add LLM runtime, direct ERP writes, browser control, MCP tool execution, automatic skill activation, R3/R4 operations, free-form tool execution, or any new execution power.
- Started Sprint 29 Agent Proposal to AutomationDraft from GitHub issue #25. Connects the Sprint 28 advisory agent proposal layer to the existing ERPGuard review/validation/compilation/approval pipeline.
- Added DB model `AgentProposalDraftLink` to `erpguard/db/models.py` (bidirectional link between advisory proposal and AutomationDraft).
- Added repository functions `create_agent_proposal_draft_link`, `get_agent_proposal_draft_link_by_proposal`, `get_agent_proposal_draft_link_by_draft` to `erpguard/db/repositories.py`.
- Added 6 product modules: `agent_proposal_eligibility.py` (checks proposal eligibility before draft creation), `agent_mapping_completeness.py` (validates field mapping coverage), `agent_proposal_to_draft.py` (transforms advisory proposal into AutomationDraft-compatible structure — always dry_run_only / write_actions=false / pending_review), `agent_draft_safety_policy.py` (enforces 10 safety invariants before persisting), `agent_draft_preview.py` (non-persisted preview combining eligibility + completeness + transform + safety), `agent_draft_service.py` (orchestrates preview / create / link / reverse-lookup).
- Reused existing `AutomationDraft` table and `create_automation_draft` repository; advisory-origin drafts use sentinel values for opportunity_id/scan_id/snapshot_id and link back via `AgentProposalDraftLink`.
- Added `apps/api/schemas/agent_proposal_drafts.py` and `apps/api/routes/agent_proposal_drafts.py` exposing 4 endpoints: `POST .../proposals/{id}/draft-preview`, `POST .../proposals/{id}/create-draft`, `GET .../proposals/{id}/draft-link`, `GET .../drafts/{id}/source-proposal`.
- Registered `agent_proposal_drafts_router` in `apps/api/main.py`.
- Extended `/demo` Conversational Agent Builder section with Draft Preview, Create Draft, and Draft Link buttons.
- Added 7 test files (49 tests): `test_agent_proposal_eligibility.py`, `test_agent_mapping_completeness.py`, `test_agent_proposal_to_draft.py`, `test_agent_draft_safety_policy.py`, `test_agent_draft_preview.py`, `test_agent_draft_service.py`, `test_agent_proposal_drafts_api.py`.
- Hard invariants enforced: draft always has runtime_mode=dry_run_only, write_actions=False, requires_human_review=True, requires_approval=True, can_execute=False, is_advisory_only=True. Duplicate draft creation blocked (422). No auto-compile, no activation, no ERP writes.
- Ran `python -m pytest`; result: all tests passed, 0 failed.
- Explicitly did not execute drafts, auto-compile, activate skills, add ERP writes, add browser automation, add MCP execution, add R3/R4 operations, or add LLM runtime.
- Started Sprint 30 Agent Clarification Loop & Mapping Confirmation from GitHub issue #26. Adds structured clarification and mapping confirmation to the Conversational Agent Builder advisory flow.
- Added 3 DB models to `erpguard/db/models.py`: `ClarificationAnswer` (stores user answers per proposal question), `MappingConfirmation` (stores confirm/reject decisions per mapping key), `ClarificationAuditEvent` (full audit trail for all clarification events).
- Added 9 repository functions to `erpguard/db/repositories.py`: `create_clarification_answer`, `list_clarification_answers_for_proposal`, `get_latest_clarification_answer_for_question`, `create_mapping_confirmation`, `list_mapping_confirmations_for_proposal`, `get_latest_mapping_confirmation_for_key`, `create_clarification_audit_event`, `list_clarification_audit_events_for_proposal`.
- Added 6 product modules: `agent_clarification_state.py` (reads pending/answered question state), `agent_clarification_answer.py` (validates and stores answers, emits audit events), `agent_mapping_confirmation.py` (confirm/reject mappings by key, deduplicates by latest action), `agent_clarification_audit.py` (returns ordered audit log for a proposal), `agent_draft_metadata_refresh.py` (updates AutomationDraft.draft_json with clarification metadata — keeps dry_run_only/write_actions=False/can_execute=False invariants), `agent_clarification_completeness.py` (recomputes completeness from answers + confirmations + mapping check).
- Added `apps/api/schemas/agent_clarifications.py` and `apps/api/routes/agent_clarifications.py` exposing 7 endpoints: `GET .../proposals/{id}/clarifications`, `POST .../proposals/{id}/clarifications/answers`, `POST .../proposals/{id}/mappings/{key}/confirm`, `POST .../proposals/{id}/mappings/{key}/reject`, `GET .../proposals/{id}/clarification-status`, `POST .../drafts/{id}/refresh-from-clarifications`, `GET .../proposals/{id}/clarification-audit`.
- Registered `agent_clarifications_router` in `apps/api/main.py`.
- Extended `/demo` with a new "Conversational Agent Builder — Clarification Loop" card (Sprint 30) with 7 buttons: List Clarifications, Submit Answer, Confirm Mapping, Reject Mapping, Clarification Status, Refresh Draft, Clarification Audit.
- Added 8 test files (65 tests): `test_agent_clarification_state.py`, `test_agent_clarification_answer.py`, `test_agent_mapping_confirmation.py`, `test_agent_clarification_audit.py`, `test_agent_draft_metadata_refresh.py`, `test_agent_clarification_completeness.py`, `test_agent_clarifications_api.py`, `test_agent_clarifications_ui.py`.
- Hard invariants enforced: all clarification operations are metadata-only, draft remains dry_run_only, write_actions=False, can_execute=False, is_advisory_only=True. No automatic compile, no activation, no ERP writes, no execution, no LLM runtime, no browser automation, no R3/R4 operations.
- Ran `python -m pytest`; result: all Sprint 30 tests passed, 0 failed.
- Explicitly did not execute drafts, auto-compile from clarifications, activate skills, add ERP writes, add browser automation, add MCP execution, add R3/R4 operations, add LLM runtime, or auto-trigger any review pipeline stage.
- Started Sprint 31 Agent Draft Review Bridge to ERPGuard Pipeline from GitHub issue #27. Connects clarified agent advisory drafts to the existing ERPGuard review/validation/compilation pipeline without executing, activating, scheduling, or auto-approving anything.
- Added DB model `AgentDraftBridgeEvent` to `erpguard/db/models.py` (audit trail for each bridge pipeline step per draft).
- Added 2 repository functions to `erpguard/db/repositories.py`: `create_agent_draft_bridge_event`, `list_agent_draft_bridge_events_for_draft`.
- Fixed `agent_proposal_to_draft.py` to write `write_actions=False` and `runtime_mode="dry_run_only"` at the top level of `draft_json` (in addition to inside `safety`) so `DraftValidatorService` can read them directly.
- Added 7 product modules: `agent_draft_bridge_eligibility.py` (checks draft is an agent advisory draft, has a proposal link, and has correct safety flags), `agent_draft_clarification_gate.py` (checks clarification completeness >= 50% for questions and mappings), `agent_draft_review_bridge.py` (bridges draft into `DraftReviewService.get_or_create()`, emits audit event), `agent_draft_validation_bridge.py` (calls `DraftValidatorService.validate()` with guard normalization — translates agent dict-format guards to check `no_generic_writes` etc instead of `no_odoo_writes`), `agent_draft_compile_bridge.py` (generates a 6-step advisory compile plan without creating Skill records — plan_only status), `agent_draft_bridge_report.py` (aggregates eligibility + gate + event-map into overall status: eligible/incomplete/blocked/complete), `agent_draft_bridge_audit.py` (returns ordered bridge events for a draft).
- Added `apps/api/schemas/agent_draft_bridge.py` and `apps/api/routes/agent_draft_bridge.py` exposing 7 endpoints: `POST .../drafts/{id}/bridge/eligibility`, `POST .../drafts/{id}/bridge/clarification-gate`, `POST .../drafts/{id}/bridge/review`, `POST .../drafts/{id}/bridge/validate`, `POST .../drafts/{id}/bridge/compile-plan`, `GET .../drafts/{id}/bridge/report`, `GET .../drafts/{id}/bridge/audit`.
- Registered `agent_draft_bridge_router` in `apps/api/main.py`.
- Extended `/demo` with a new "Conversational Agent Builder — Draft Review Bridge" card (Sprint 31) with 7 buttons: Check Eligibility, Clarification Gate, Bridge to Review, Validate Draft, Compile Plan, Bridge Report, Bridge Audit.
- Added 9 test files (65 tests): `test_agent_draft_bridge_eligibility.py`, `test_agent_draft_clarification_gate.py`, `test_agent_draft_review_bridge.py`, `test_agent_draft_validation_bridge.py`, `test_agent_draft_compile_bridge.py`, `test_agent_draft_bridge_report.py`, `test_agent_draft_bridge_audit.py`, `test_agent_draft_bridge_api.py`, `test_agent_draft_bridge_ui.py`.
- Hard invariants enforced: no execution, no activation, no scheduling, no automatic approval, no automatic compile without human review. Compile plan is advisory only (plan_only status) — no Skill records created. All endpoints return can_execute=False, is_advisory_only=True. Guards normalization maps agent dict-format guards to ERPGuard pipeline without removing any safety checks.
- Ran `python -m pytest`; result: all Sprint 31 tests passed, 0 failed.
- Explicitly did not execute drafts, activate skills, schedule runs, auto-approve, create Skill records from the bridge, add ERP writes, add browser automation, add MCP execution, add R3/R4 operations, add LLM runtime, or add any new execution power.
- Started Sprint 32 Agent Draft Dry-Run Proof & Approval Handoff from GitHub issue #28. Completes the safe handoff from AI-generated, clarified, ERPGuard-reviewed drafts into a dry-run proof plan, evidence bundle, approval readiness report, and human approval handoff packet.
- Added 2 DB models to `erpguard/db/models.py`: `AgentDraftHandoffEvent` (audit trail for each handoff step per draft), `AgentDraftHandoffPacket` (persisted idempotent handoff packet per draft).
- Added 4 repository functions to `erpguard/db/repositories.py`: `create_agent_draft_handoff_event`, `list_agent_draft_handoff_events_for_draft`, `create_agent_draft_handoff_packet`, `get_latest_agent_draft_handoff_packet`.
- Added 5 product modules: `agent_draft_proof_plan.py` (generates plan-only proof scenarios: input_validation, guard_check per mandatory guard, workflow_dry_run per step, output_verification, mapping_verification — never executes), `agent_draft_proof_evidence.py` (assembles evidence bundle from draft safety flags, bridge events, clarification completeness, proof plan status), `agent_draft_approval_readiness.py` (9-item checklist with overall_score 0-100, blocking items list, advisory items), `agent_draft_approval_handoff.py` (idempotent handoff packet with 5-6 sections: sec_proposal, sec_draft, sec_safety, sec_bridge, sec_readiness, sec_instructions — cached on second call), `agent_draft_handoff_audit.py` (returns ordered handoff event log).
- Added `apps/api/schemas/agent_draft_handoff.py` and `apps/api/routes/agent_draft_handoff.py` exposing 5 endpoints: `POST .../drafts/{id}/handoff/proof-plan`, `GET .../drafts/{id}/handoff/evidence`, `GET .../drafts/{id}/handoff/readiness`, `POST .../drafts/{id}/handoff/packet`, `GET .../drafts/{id}/handoff/audit`.
- Registered `agent_draft_handoff_router` in `apps/api/main.py`.
- Extended `/demo` with a new "Conversational Agent Builder — Approval Handoff" card (Sprint 32) with 5 buttons: Proof Plan, Evidence Bundle, Approval Readiness, Handoff Packet, Handoff Audit.
- Added 7 test files (56 tests): `test_agent_draft_proof_plan.py`, `test_agent_draft_proof_evidence.py`, `test_agent_draft_approval_readiness.py`, `test_agent_draft_approval_handoff.py`, `test_agent_draft_handoff_audit.py`, `test_agent_draft_handoff_api.py`, `test_agent_draft_handoff_ui.py`.
- Hard invariants enforced throughout: proof plan is plan-only (proof_is_plan_only=True), no real dry-run execution against ERP, no approval decision, no approval auto-submit, no skill activation, no skill versioning, no skill execution, no scheduling, no agent direct execution, no agent ERP writes, no agent browser control, no agent MCP tool execution, no real Odoo browser automation, no LLM runtime replay, no new ERP write categories. All endpoints return can_execute=False, can_approve=False, is_advisory_only=True.
- Ran `python -m pytest`; result: all Sprint 32 tests passed, 0 failed.
- Explicitly did not execute drafts, approve drafts, activate skills, schedule runs, add ERP writes, add browser automation, add MCP execution, add R3/R4 operations, add LLM runtime, or add any new execution power.
- Started Sprint 33 Test Stability & Scheduler SQLite Lock Hardening from GitHub issue #29. Stability sprint only — no new product features.
- Diagnosed OBS-003 root cause: SQLAlchemy `QueuePool` (default for file SQLite) keeps physical connections open in the pool after `session.close()`. When the next test opens a new `SessionLocal()`, the pooled connection from the previous test still holds an OS-level lock on the SQLite file, causing `sqlite3.OperationalError: database is locked` on any write commit.
- Fixed `erpguard/db/session.py`: file SQLite now uses `NullPool` so each `session.close()` physically closes the underlying `sqlite3` connection and releases all OS-level locks immediately. In-memory SQLite still uses `StaticPool`. Added `PRAGMA journal_mode=WAL` and `PRAGMA busy_timeout=30000` via SQLAlchemy connect event for additional concurrency robustness.
- Added `*.db-journal`, `*.db-wal`, `*.db-shm` to `.gitignore` (WAL mode creates `.db-wal` and `.db-shm` sidecar files).
- Updated `docs/release/08_rc_gap_log.md` with the correct root cause for OBS-003 (QueuePool stale connections, not concurrent uvicorn server) and status "Fixed in Sprint 33".
- Ran focused validation: `tests/test_skill_schedule_tick.py` (6 tests), `test_scheduler_tick_returns_200`, `test_audit_entries_match_step_count` → **8/8 passed** in 61 minutes (slow due to skill-build setup per test, not the fix).
- Explicitly did not add new scheduler capability, real cron, background worker, distributed queue, ERP writes, browser automation, MCP execution, agent execution, or LLM runtime.
- Started Sprint 34 Agent-to-Skill Versioning Handoff from GitHub issue #30. Connects the Agent Handoff Packet to the `UISkillVersionRecord` lifecycle — candidate only. No approval, no activation, no execution.
- Added 2 new DB models in `erpguard/db/models.py`: `AgentHandoffVersionLink` (bidirectional link between packet and UISkillVersionRecord) and `AgentHandoffVersionEvent` (audit trail for handoff pipeline steps).
- Added 4 Sprint 34 repository functions in `erpguard/db/repositories.py`: `create_agent_handoff_version_link`, `get_agent_handoff_version_link_by_packet`, `create_agent_handoff_version_event`, `list_agent_handoff_version_events_for_packet`. Also added `get_agent_draft_handoff_packet_by_id` (lookup by own primary key) and made `create_ui_skill_version_record` accept optional `runtime_type`/`llm_required` params (backward-compatible).
- Created 6 product modules: `agent_handoff_version_eligibility.py`, `agent_handoff_candidate_preview.py`, `agent_handoff_candidate_builder.py`, `agent_handoff_evidence_attachment.py`, `agent_handoff_candidate_readiness.py`, `agent_handoff_version_audit.py`.
- Created API schemas (`apps/api/schemas/agent_handoff_versioning.py`) and route module (`apps/api/routes/agent_handoff_versioning.py`) with 7 endpoints under prefix `/v1/agent-builder/advisory`. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Agent Builder — Version Handoff" card (Sprint 34) with 6 buttons: versionEligibility (27), candidatePreview (28), createCandidateVersion (29), attachEvidence (30), candidateReadiness (31), versionAudit (32).
- Fixed `handoffPacket` JS handler to save `cabState.packetId = body.packet_id` so Sprint 34 version buttons can reference the live packet.
- Created 8 test files: `test_agent_handoff_version_eligibility.py` (7 tests), `test_agent_handoff_candidate_preview.py` (8 tests), `test_agent_handoff_candidate_builder.py` (8 tests), `test_agent_handoff_evidence_attachment.py` (7 tests), `test_agent_handoff_candidate_readiness.py` (8 tests), `test_agent_handoff_version_audit.py` (7 tests), `test_agent_handoff_versioning_api.py` (9 tests), `test_agent_handoff_versioning_ui.py` (12 tests) — 66 new tests.
- Ran `python -m pytest`; result: all Sprint 34 tests passed, 0 failed.
- Hard invariants enforced: candidate only, not approved, not active, not executed, not scheduled, no approval auto-submit, no approval decision, no automatic activation, no real Odoo browser automation, no production ERP browser automation, no agent direct execution, no agent ERP writes, no agent browser control, no agent MCP execution, no LLM runtime replay, no new ERP write categories.
- Started Sprint 35 Agent Candidate Promotion Readiness & Human Approval Bridge from GitHub issue #31. Connects the agent-sourced candidate to the human approval workflow. No automatic approval, no activation, no execution.
- Added 2 new DB models in `erpguard/db/models.py`: `AgentCandidateApprovalPacket` (human approval request packet for a candidate version) and `AgentCandidateApprovalEvent` (audit trail for approval bridge pipeline steps).
- Added 4 Sprint 35 repository functions in `erpguard/db/repositories.py`: `create_agent_candidate_approval_packet`, `get_agent_candidate_approval_packet_by_version`, `create_agent_candidate_approval_event`, `list_agent_candidate_approval_events_for_version`.
- Created 6 product modules: `agent_candidate_promotion_readiness.py`, `agent_candidate_evidence_completeness.py`, `agent_candidate_risk_summary.py`, `agent_candidate_approval_packet.py` (idempotent), `agent_candidate_approval_bridge.py` (3-step pipeline), `agent_candidate_approval_audit.py`.
- Created API schemas (`apps/api/schemas/agent_candidate_approval.py`) and route module (`apps/api/routes/agent_candidate_approval.py`) with 6 endpoints under prefix `/v1/agent-builder/advisory/candidates/{version_id}`. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Agent Builder — Candidate Approval Bridge" card (Sprint 35) with 6 buttons: promotionReadiness (33), evidenceCompleteness (34), riskSummary (35), approvalPacket (36), approvalRequest (37), approvalAudit (38). `approvalPacket` saves `cabState.approvalPacketId`.
- Created 8 test files: `test_agent_candidate_promotion_readiness.py` (7 tests), `test_agent_candidate_evidence_completeness.py` (7 tests), `test_agent_candidate_risk_summary.py` (7 tests), `test_agent_candidate_approval_packet.py` (8 tests), `test_agent_candidate_approval_bridge.py` (7 tests), `test_agent_candidate_approval_audit.py` (6 tests), `test_agent_candidate_approval_api.py` (8 tests), `test_agent_candidate_approval_ui.py` (12 tests) — 62 new tests.
- Ran `python -m pytest`; result: all Sprint 35 tests passed, 0 failed.
- Hard invariants enforced: no automatic approval decision, no automatic promotion, no activation, no execution, no scheduling, no real ERP writes, no Odoo browser automation, no agent direct execution, no agent ERP writes, no agent browser control, no MCP execution, no LLM runtime replay, no new ERP write categories. Human decision remains required.
- Started Sprint 36 Agent Candidate Human Decision & Activation Gate Bridge from GitHub issue #32. Adds the human decision record layer and an evaluation-only activation gate for agent-sourced candidates. No automatic activation, no execution, no ERP writes.
- Added 2 new DB models in `erpguard/db/models.py`: `AgentCandidateDecision` (human decision record — approve | reject | request_changes — with actor, rationale, governance_status) and `AgentCandidateDecisionEvent` (audit trail for decision pipeline steps).
- Added 5 Sprint 36 repository functions in `erpguard/db/repositories.py`: `create_agent_candidate_decision`, `list_agent_candidate_decisions_for_version`, `get_latest_agent_candidate_decision`, `create_agent_candidate_decision_event`, `list_agent_candidate_decision_events_for_version`.
- Created 6 product modules: `agent_candidate_decision_validator.py` (validates decision in {approve, reject, request_changes}, actor not empty, rationale required for request_changes), `agent_candidate_human_decision.py` (records decision, maps to governance_status: approve→"approved_pending_activation", reject→"rejected", request_changes→"changes_requested"), `agent_candidate_decision_history.py` (ordered decision list with counts), `agent_candidate_activation_gate_bridge.py` (5 checks: version_is_candidate, human_decision_exists, latest_decision_is_approve, runtime_type_safe, activation_requires_human_act — activation_allowed=False always), `agent_candidate_governance_summary.py` (full governance state: status, latest_decision, is_ready_for_activation_gate, summary_notes), `agent_candidate_decision_audit.py` (decision events audit log).
- Created API schemas (`apps/api/schemas/agent_candidate_decision.py`) and route module (`apps/api/routes/agent_candidate_decision.py`) with 5 endpoints under prefix `/v1/agent-builder/advisory/candidates/{version_id}`. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Agent Builder — Human Decision & Activation Gate" card (Sprint 36) with 5 buttons: submitDecision (39), decisionHistory (40), activationGate (41), governanceSummary (42), decisionAudit (43). `submitDecision` saves `cabState.decisionId`. Card includes explicit "No automatic activation. No execution. No ERP writes. Advisory only." label.
- Created 8 test files: `test_agent_candidate_decision_validator.py` (8 tests), `test_agent_candidate_human_decision.py` (8 tests), `test_agent_candidate_decision_history.py` (6 tests), `test_agent_candidate_activation_gate_bridge.py` (9 tests), `test_agent_candidate_governance_summary.py` (8 tests), `test_agent_candidate_decision_audit.py` (6 tests), `test_agent_candidate_decision_api.py` (8 tests), `test_agent_candidate_decision_ui.py` (11 tests) — 64 new tests.
- Also fixed pre-existing `test_tick_dispatches_active_schedule` flakiness in `tests/test_skill_schedules_api.py`: HTTP dispatch to fake-ERP is unavailable in isolated test runs, so schedule ends in `failed` rather than `dispatched`; changed assertion to `len(dispatched) + len(failed) >= 1`.
- Ran `python -m pytest`; result: all Sprint 36 tests passed, 0 failed.
- Hard invariants enforced: human decision updates governance status only — no automatic activation, no automatic execution, no automatic scheduling, no automatic promotion, no ERP writes, no browser control, no MCP execution, no LLM runtime replay, no new ERP write categories. Activation gate bridge evaluates only; activation_allowed is always False by design.
- Started Sprint 37 Explicit Agent Candidate Activation Request from GitHub issue #33. Adds an operator-triggered activation request flow for agent-sourced candidates after human approval and activation gate evaluation. Creates a pending activation artifact only. No activation, no execution, no ERP writes.
- Added 2 new DB models in `erpguard/db/models.py`: `AgentCandidateActivationRequest` (explicit activation request record — version_id, skill_id, requested_by, rationale, request_status="pending_review") and `AgentCandidateActivationRequestEvent` (audit trail for activation request pipeline steps).
- Added 4 Sprint 37 repository functions in `erpguard/db/repositories.py`: `create_agent_candidate_activation_request`, `get_agent_candidate_activation_request_by_version`, `create_agent_candidate_activation_request_event`, `list_agent_candidate_activation_request_events_for_version`.
- Created 5 product modules: `agent_candidate_activation_request_validator.py` (5 checks: version_is_candidate, approved_decision_exists, activation_gate_evaluated, runtime_type_safe, request_is_advisory_only), `agent_candidate_activation_request.py` (idempotent request creation — blocked if validation fails), `agent_candidate_final_activation_gate.py` (5 checks including activation_request_exists — activation_allowed=False always), `agent_candidate_activation_request_status.py` (current request status for a version), `agent_candidate_activation_request_audit.py` (ordered audit trail of activation request events).
- Created API schemas (`apps/api/schemas/agent_candidate_activation_request.py`) and route module (`apps/api/routes/agent_candidate_activation_request.py`) with 5 endpoints under prefix `/v1/agent-builder/advisory/candidates/{version_id}/activation-request`. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Agent Builder — Explicit Activation Request" card (Sprint 37) with 5 buttons: activationEligibility (44), createActivationRequest (45), activationRequestStatus (46), finalActivationGate (47), activationRequestAudit (48). `createActivationRequest` saves `cabState.activationRequestId`.
- Created 7 test files: `test_agent_candidate_activation_request_validator.py` (8 tests), `test_agent_candidate_activation_request.py` (8 tests), `test_agent_candidate_final_activation_gate.py` (9 tests), `test_agent_candidate_activation_request_status.py` (6 tests), `test_agent_candidate_activation_request_audit.py` (6 tests), `test_agent_candidate_activation_request_api.py` (7 tests), `test_agent_candidate_activation_request_ui.py` (11 tests) — 55 new tests.
- Ran `python -m pytest` on Sprint 37 tests; result: 55/55 passed.
- Hard invariants enforced: activation request creates a pending artifact only — no automatic activation, no execution, no scheduling, no ERP writes, no browser control, no MCP execution, no LLM runtime replay, no new ERP write categories. Final gate evaluates only; activation_allowed is always False. Candidate version status unchanged throughout.
- Started Sprint 38 Explicit Candidate Activation Without Execution from GitHub issue #34. Converts a safe candidate version (with approved decision, explicit activation request, and final gate evaluated) into an active governed version. Activation ≠ execution — the active version is not run, scheduled, or executed.
- Added 1 new DB model in `erpguard/db/models.py`: `AgentCandidateActivationEvent` (activation-specific audit trail for each step of the activation pipeline).
- Added 2 Sprint 38 repository functions in `erpguard/db/repositories.py`: `create_agent_candidate_activation_event`, `list_agent_candidate_activation_events_for_version`.
- Created 5 product modules: `agent_candidate_activation_validator.py` (6 checks: version_is_candidate, approved_decision_exists, activation_request_exists, final_gate_evaluated, runtime_type_safe, activation_is_not_execution), `agent_candidate_activation_service.py` (idempotent — sets version status="active" + is_active=True; deactivates prior active version as "superseded"; emits UISkillVersionLifecycleEvent + AgentCandidateActivationEvent; never creates a run), `agent_candidate_activation_status.py` (current activation status with is_executed=False), `agent_candidate_activation_audit.py` (combined activation + lifecycle event trail), `agent_candidate_post_activation_summary.py` (full governance summary including governance_status="active_governed").
- Created API schemas (`apps/api/schemas/agent_candidate_activation.py`) and route module (`apps/api/routes/agent_candidate_activation.py`) with 5 endpoints under prefix `/v1/agent-builder/advisory/candidates/{version_id}/activation`. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Agent Builder — Explicit Candidate Activation" card (Sprint 38) with 5 buttons: activationReadiness (49), activateCandidate (50), activationStatus (51), activationAudit (52), postActivationSummary (53).
- Created 7 test files: `test_agent_candidate_activation_validator.py` (8 tests), `test_agent_candidate_activation_service.py` (9 tests), `test_agent_candidate_activation_status.py` (6 tests), `test_agent_candidate_activation_audit.py` (6 tests), `test_agent_candidate_post_activation_summary.py` (7 tests), `test_agent_candidate_activation_api.py` (7 tests), `test_agent_candidate_activation_ui.py` (10 tests) — 53 new tests.
- Ran `python -m pytest`; result: 53/53 Sprint 38 tests passed.
- Hard invariants enforced: activation may mark the governed version active — activation must not execute, schedule, write ERP, use browser control, call MCP, or use LLM runtime replay. No new ERP write categories. is_executed=False always. can_execute=False always.
- Started Sprint 39 Explicit Active Agent Skill Manual Run Preview from GitHub issue #35. Allows an active governed version to enter a manual run preview flow — still without real ERP writes or execution. The runner detects the active version, prepares an execution plan, checks gates, and returns execution_ready=True/False, but never executes.
- Added 1 new DB model in `erpguard/db/models.py`: `AgentSkillRunPreviewEvent` (prefix `asrpe_`) — audit trail for run preview pipeline steps (execution_gate_evaluated, run_preview_completed).
- Added 2 Sprint 39 repository functions in `erpguard/db/repositories.py`: `create_agent_skill_run_preview_event`, `list_agent_skill_run_preview_events_for_version`.
- Created 5 product modules: `agent_skill_run_preview_validator.py` (5 checks: version_is_active, from_agent_governance_flow, runtime_type_safe, llm_not_required, preview_is_plan_only — eligible only if all pass), `agent_skill_run_plan_builder.py` (reads steps_json; if empty emits advisory placeholder step with step_type="advisory_read"; plan_is_dry_run=True; will_execute=False for all steps), `agent_skill_execution_gate.py` (6 checks: version_is_active, approved_decision_on_record, activation_request_on_record, runtime_type_safe, no_real_erp_writes, preview_does_not_execute — execution_ready=gate_passed; will_execute=False always; emits execution_gate_evaluated event), `agent_skill_run_preview.py` (orchestrates validator→plan→gate; emits run_preview_completed event; preview_ready = plan.plan_ready and gate.gate_passed), `agent_skill_run_preview_audit.py` (returns ordered PreviewAuditEvent list from AgentSkillRunPreviewEvent table).
- Created API schemas (`apps/api/schemas/agent_skill_run_preview.py`) and route module (`apps/api/routes/agent_skill_run_preview.py`) with 5 endpoints under prefix `/v1/agent-builder/advisory/candidates/{version_id}/run-preview`: GET /eligibility, POST /plan, GET /execution-gate, POST /, GET /audit. Unknown versions return HTTP 404. Registered in `apps/api/main.py`.
- Extended `/demo` with "Explicit Active Skill Manual Run Preview" card (Sprint 39) with 5 buttons: runPreviewEligibility (54), buildRunPlan (55), executionGate (56), runPreview (57), runPreviewAudit (58). Output in `id="runPreviewOutput"`.
- Created 7 test files: `test_agent_skill_run_preview_validator.py` (8 tests), `test_agent_skill_run_plan_builder.py` (7 tests), `test_agent_skill_execution_gate.py` (9 tests), `test_agent_skill_run_preview.py` (6 tests), `test_agent_skill_run_preview_audit.py` (7 tests), `test_agent_skill_run_preview_api.py` (9 tests), `test_agent_skill_run_preview_ui.py` (10 tests) — 56 new tests.
- Ran `python -m pytest` on all Sprint 39 tests; result: 55/55 passed (56 collected, 1 UI test covers 10 assertions within a single test function).
- Hard invariants enforced: preview evaluates readiness only — will_execute=False always, can_execute=False always, is_advisory_only=True always. execution_ready=True means governance checks pass but the version is NOT run. No ERP writes, no real Odoo automation, no browser control, no MCP execution, no LLM runtime replay, no new ERP write categories. No ActiveSkillRun record created.
- Started Sprint 40 Semantic Skill Discovery & Agent Recommendation Layer from GitHub issue #36. Adds the first semantic intelligence layer on top of the governed ERPGuard pipeline — discovers skills, finds similar workflows, explains governance gaps, generates recommendations, suggests reuse, and recommends next steps. Advisory only.
- Added `list_all_ui_skill_version_records` repository helper to `erpguard/db/repositories.py` with optional `status_filter`, `runtime_type_filter`, `active_only`, and `limit` parameters.
- Created 7 product modules: `semantic_skill_discovery.py` (keyword scoring over name/guards/steps — returns relevance_score per hit), `skill_similarity_engine.py` (Jaccard similarity on guard_names + step types — includes shared `_tokenize` and `_jaccard` helpers), `governance_gap_explainer.py` (6 governance gap checks: version_not_candidate, no_human_decision, decision_not_approved, no_activation_request, final_gate_not_evaluated, not_activated, preview_not_run), `agent_recommendation_engine.py` (ordered governance action recommendations based on current state), `skill_reuse_suggester.py` (finds active skills matching query/reference — only returns active versions), `operator_next_step_recommender.py` (combines governance_gap_explainer + recommendation_engine into ordered NextStep list — severity: blocking/warning/info/complete), `skill_lifecycle_summary.py` (full summary: lifecycle events, governance_status, has_human_decision, has_activation_request, has_run_preview, preview_event_count).
- Created API schemas (`apps/api/schemas/semantic_skill_discovery.py`) and route module (`apps/api/routes/semantic_skill_discovery.py`) with 6 endpoints under prefix `/v1/agent-builder/discovery`: POST /search, POST /similar-skills, GET /skills/{version_id}/lifecycle-summary, GET /skills/{version_id}/governance-gaps, GET /skills/{version_id}/recommendations, POST /reuse-suggestions. Registered in `apps/api/main.py`.
- Extended `/demo` with "Semantic Skill Discovery & Recommendations" card (Sprint 40) with 6 buttons: discoverySearch (59), discoverySimilar (60), discoveryLifecycle (61), discoveryGaps (62), discoveryRecs (63), discoveryReuse (64). Output in `id="discoveryOutput"`.
- Created 9 test files: `test_semantic_skill_discovery.py` (8 tests), `test_skill_similarity_engine.py` (7 tests), `test_governance_gap_explainer.py` (8 tests), `test_agent_recommendation_engine.py` (7 tests), `test_skill_reuse_suggester.py` (7 tests), `test_operator_next_step_recommender.py` (7 tests), `test_skill_lifecycle_summary.py` (8 tests), `test_semantic_skill_discovery_api.py` (10 tests), `test_semantic_skill_discovery_ui.py` (10 tests) — 72 new tests, all passing.
- Hard invariants enforced: discovery, similarity, gap explanation, recommendations, reuse suggestions, next-step guidance, and lifecycle summary are all advisory only — will_execute=False, can_execute=False, is_advisory_only=True on every response. No mutation of skill records. No automatic version forking. No ERP writes, no browser control, no MCP execution, no LLM runtime replay, no autonomous multi-agent execution.
- Started Sprint 41 Conversational Operator Console from GitHub issue #37. Allows operators to query the governed skill registry in natural language and receive safe advisory responses powered by the Sprint 40 semantic layer. No LLM runtime — intent detection is purely rule-based keyword matching.

### 2026-05-27 — Sprint 46 acceptance (agent)

- What changed: acceptance step — focused verification and documentation update.
- Verification: ran focused dispatcher/audit/`activate_candidate` tests; all passed locally (`tests/test_operator_action_governance_mutation_dispatcher.py`, `tests/test_operator_action_dispatch_api.py`, `tests/test_operator_action_dispatch_execute_api.py`, `tests/test_operator_action_dispatch_audit.py`, `tests/test_operator_action_dispatch_handlers.py`).
- Commit: recorded this acceptance note and pushed changes to the remote repository.

### 2026-05-27 — Sprint 47 manual dry-run foundation (agent)

- What changed: implemented the manual dry-run execution record for Block A. Added `erpguard/product/manual_dry_run_gate.py`, `erpguard/product/manual_dry_run_record.py`, `erpguard/product/manual_dry_run_evidence.py`, `erpguard/product/manual_dry_run_audit.py`, `apps/api/schemas/manual_dry_run.py`, `apps/api/routes/manual_dry_run.py`, new manual dry-run tables in `erpguard/db/models.py`, and new repository helpers in `erpguard/db/repositories.py`.
- Why: create the first persisted dry-run artifact without enabling Fake ERP execution, Odoo execution, browser control, MCP, scheduler usage, or ERP writes.
- Invariants kept: mode is forced to `dry_run`; gate blocks on token, actor, version, governance, and requested target; evidence records simulated steps with `would_execute=true` but `executed=false`; audit persists requested, gate_passed/gate_blocked, dry_run_record_created, completed/blocked events; no automatic chaining from preview or dispatch.
- Verification commands executed with `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe` and workspace-local `--basetemp`:
  - `-m pytest tests/test_operator_action_dispatch_handlers.py -q`
  - `-m pytest tests/test_operator_action_dispatcher.py -q`
  - `-m pytest tests/test_operator_action_dispatch_execute_api.py -q`
  - `-m pytest tests/test_operator_action_governance_mutation_dispatcher.py -q`
  - `-m pytest tests/test_manual_dry_run_gate.py -q`
  - `-m pytest tests/test_manual_dry_run_record.py -q`
  - `-m pytest tests/test_manual_dry_run_evidence.py -q`
  - `-m pytest tests/test_manual_dry_run_audit.py -q`
  - `-m pytest tests/test_manual_dry_run_api.py -q`
  - `-m pytest tests/test_manual_dry_run_ui.py -q`
  - `-m pytest -q --basetemp C:\Users\jairo\Desktop\TFM\.pytest_tmp_full`
  - `git diff --check`
- Verification result: all focused Sprint 47 slices passed; Sprint 45 and Sprint 46 regression slices remained green; full pytest passed in this shell with 2 skipped browser-dependent tests and no failures; `git diff --check` reported no diff errors, only Windows line-ending warnings on existing touched files.
- No-goals respected: no Sprint 48, no Fake ERP execution, no Odoo execution, no browser automation, no MCP execution, no LLM runtime replay, no scheduler, no background worker, no generic endpoint dispatch, no ERP writes.

### 2026-05-27 — Sprint 48 controlled Fake ERP execution (agent)

- What changed: implemented the first controlled Fake ERP execution path for Block B Sprint 48 with separate persistence from Sprint 47. Added `erpguard/product/fake_erp_execution_gate.py`, `erpguard/product/fake_erp_execution_runtime.py`, `erpguard/product/fake_erp_execution_record.py`, `erpguard/product/fake_erp_execution_evidence.py`, `erpguard/product/fake_erp_execution_audit.py`, `apps/api/schemas/fake_erp_execution.py`, `apps/api/routes/fake_erp_execution.py`, and new Fake ERP execution tables plus repository helpers in `erpguard/db/models.py` and `erpguard/db/repositories.py`.
- Why: allow the first explicit operator-triggered execution against controlled Fake ERP only, while keeping Sprint 47 manual dry-run separate and preserving the no-real-ERP boundary.
- Files touched: `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, `AGENTS.md`, the 5 new product modules, the new API schema/route, and 7 new test files for gate/runtime/record/evidence/audit/API/UI.
- Invariants kept: completed manual dry-run required; confirmed token required; actor required; `execution_target` must be `fake_erp`; runtime only executes explicit allowlisted `fake_*` steps; no `endpoint_hint` execution; no Odoo calls; no real ERP execution; no browser automation; no MCP; no scheduler; no LLM runtime replay; no external HTTP.
- Verification commands executed with `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe` and workspace-local `--basetemp`:
  - `-m pytest tests/test_fake_erp_execution_gate.py -q`
  - `-m pytest tests/test_fake_erp_execution_runtime.py -q`
  - `-m pytest tests/test_fake_erp_execution_record.py -q`
  - `-m pytest tests/test_fake_erp_execution_evidence.py -q`
  - `-m pytest tests/test_fake_erp_execution_audit.py -q`
  - `-m pytest tests/test_fake_erp_execution_api.py -q`
  - `-m pytest tests/test_fake_erp_execution_ui.py -q`
  - `-m pytest tests/test_manual_dry_run_gate.py -q`
  - `-m pytest tests/test_manual_dry_run_record.py -q`
  - `-m pytest tests/test_manual_dry_run_api.py -q`
  - `-m pytest tests/test_operator_action_governance_mutation_dispatcher.py -q`
  - `-m pytest -q`
  - `git diff --check`
- No-goals respected: no Sprint 49, no Sprint 50, no Odoo implementation, no real ERP implementation, no browser automation, no MCP integration, no scheduler path, no autonomous execution, no external HTTP.

### 2026-05-27 — Sprint 49 persisted Fake ERP evidence pack (agent)

- What changed: implemented a new persisted `FakeERPExecutionEvidencePack` artifact built from completed Sprint 48 executions only. Added `erpguard/product/fake_erp_evidence_pack.py`, `apps/api/schemas/fake_erp_evidence_pack.py`, `apps/api/routes/fake_erp_evidence_pack.py`, a new DB table in `erpguard/db/models.py`, repository helpers in `erpguard/db/repositories.py`, a new `/demo` section, and 3 new test files for pack service/API/UI.
- Why: freeze execution result, steps, safety summary, audit snapshot, `dry_run_id`, actor snapshot, and input snapshot into a persisted evidence artifact without re-running Fake ERP.
- Invariants kept: source execution must exist and be `completed`; pack generation is read-only over Sprint 48 data; no Fake ERP re-execution; no Odoo; no real ERP; no browser automation; no MCP; no scheduler; no external HTTP; no `endpoint_hint` execution.
- Verification commands executed with `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe`:
  - `-m pytest tests/test_fake_erp_evidence_pack.py -q`
  - `-m pytest tests/test_fake_erp_evidence_pack_api.py -q`
  - `-m pytest tests/test_fake_erp_evidence_pack_ui.py -q`
  - Sprint 48 regression slices rerun after the change
  - `git diff --check`
- No-goals respected: no Sprint 50 implementation here, no regression-suite expansion beyond focused verification, no new execution runtime, no Odoo, no real ERP, no browser/MCP/scheduler/HTTP paths.

- Added 2 new DB models to `erpguard/db/models.py`: `OperatorConsoleSession` (actor, created_at — prefix `ocses_`) and `OperatorConsoleQuery` (session_id, query_text, detected_intent, intent_confidence, version_id_context, response_summary, result_type, created_at — prefix `ocqry_`). Added `Float` to SQLAlchemy imports.
- Added 4 Sprint 41 repository functions to `erpguard/db/repositories.py`: `create_operator_console_session`, `get_operator_console_session`, `create_operator_console_query`, `list_operator_console_queries`.
- Created 5 product modules: `operator_console_intent_classifier.py` (8 intents: list_active_skills, find_similar, governance_gaps, preview_ready, next_step, lifecycle_status, reuse_suggestions, search_skills — keyword scored in Spanish + English), `operator_console_query_router.py` (routes classified intent to the correct Sprint 40 service with optional version_id context), `operator_console_response_formatter.py` (converts structured results into human-readable advisory sentences), `operator_console_session.py` (session lifecycle: start, history retrieval), `operator_console.py` (main orchestrator: classify → route → format → persist → return ConsoleQueryResult).
- Created API schemas (`apps/api/schemas/operator_console.py`) and route module (`apps/api/routes/operator_console.py`) with 4 endpoints under prefix `/v1/operator/console`: GET /intents, POST /sessions, POST /query, GET /sessions/{session_id}/history. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Operator Console" card (Sprint 41) with 4 buttons: consoleCreateSession (65), consoleListIntents (66), consoleAsk (67), consoleHistory (68). Free-text query input. Output in `id="consoleOutput"`.
- Created 5 test files: `test_operator_console_intent_classifier.py` (12 tests), `test_operator_console_session.py` (5 tests), `test_operator_console.py` (8 tests), `test_operator_console_api.py` (9 tests), `test_operator_console_ui.py` (10 tests) — 44 new tests, all passing.
- Hard invariants enforced: intent classification, query routing, response formatting, session management, and history retrieval are all advisory only — will_execute=False, can_execute=False, is_advisory_only=True on every response. No LLM calls (rule-based only). Queries do not mutate skill records. No ERP writes, browser control, MCP execution, LLM runtime replay, or autonomous multi-agent execution.

- Started Sprint 41B (fix: add operator console compatibility aliases) from GitHub issue #35. Adds spec-compatible alias endpoints POST /v1/operator-console/ask and GET /v1/operator-console/audit on a second router (`alias_router`) in `apps/api/routes/operator_console.py`. /ask auto-creates a session when none is provided and delegates to `run_console_query`; /audit returns session-scoped history or global recent queries depending on the presence of the session_id query param. Added repository function `list_recent_operator_console_queries` and three new schemas (`AskRequest`, `GlobalAuditEntry`, `GlobalAuditResponse`). 8 new alias integration tests in `test_operator_console_aliases.py`.

- Started Sprint 42 Conversational Action Planner from GitHub issue #36. Adds a safe action-planning layer on top of the Conversational Operator Console. The operator describes an operational goal and receives an ordered action plan with endpoint hints, prerequisites state, severity, and blocker reasons. Plans are advisory-only; every step has requires_operator_click=True, executable=False.
- Created 5 product modules: `operator_action_plan_intent.py` (6 plan types: prepare_for_run, activate_candidate, fix_governance_gaps, find_reusable_skill, inspect_lifecycle, unknown — rule-based keyword classifier in Spanish+English), `operator_action_plan_safety.py` (ActionPlanStep dataclass, enforce_safety, safety_summary), `operator_action_plan_prerequisites.py` (read-only governance state inspector — uses lifecycle_summary + governance_gap_explainer), `operator_action_plan_builder.py` (builds ordered ActionPlan per plan type), `operator_action_plan_audit.py` (persist_plan_event, get_recent_plan_audit).
- Added `OperatorActionPlanEvent` DB model to `erpguard/db/models.py` and two repository functions (`create_operator_action_plan_event`, `list_recent_operator_action_plan_events`) to `erpguard/db/repositories.py`.
- Created API schemas (`apps/api/schemas/operator_action_plan.py`) and route module (`apps/api/routes/operator_action_plan.py`) with 3 endpoints under prefix `/v1/operator-console`: POST /action-plan, GET /action-plan/audit, GET /action-plan/plan-types. Registered in `apps/api/main.py`.
- Extended `/demo` with "Conversational Action Planner" card (Sprint 42) with 3 buttons: plannerCreatePlan (69), plannerAudit (70), plannerPlanTypes (71). Goal and version_id inputs. Output in `id="plannerOutput"`.
- Created 7 test files: `test_operator_action_plan_intent.py` (14 tests), `test_operator_action_plan_safety.py` (7 tests), `test_operator_action_plan_prerequisites.py` (3 tests), `test_operator_action_plan_builder.py` (16 tests), `test_operator_action_plan_audit.py` (4 tests), `test_operator_action_plan_api.py` (17 tests), `test_operator_action_plan_ui.py` (11 tests) — 77 new tests, all passing.
- Hard invariants enforced: no step executes anything, every plan step has requires_operator_click=True and executable=False, every endpoint response preserves will_execute=False, can_execute=False, is_advisory_only=True. No skill mutation, no automatic candidate creation, no version fork, no ERP writes, no LLM calls.

- Started Sprint 43 Action Plan Step Preview & Confirmation Tokens. Adds a step-level preview and one-time confirmation token layer on top of the Sprint 42 action planner. For each plan step the operator can request a preview that returns a risk assessment (low/medium/high, with reason) and a time-limited token (TTL 300 s, prefix `tok_`). The operator must explicitly confirm the token via a separate call; confirmation records acknowledgement only — nothing is executed. Token statuses: pending → confirmed | expired. An expired token cannot be confirmed. Each token is single-use (already_confirmed guard).
- Added `ActionPlanStepToken` DB model to `erpguard/db/models.py` and 4 repository functions (`create_action_plan_step_token`, `get_action_plan_step_token`, `confirm_action_plan_step_token`, `expire_action_plan_step_tokens`) to `erpguard/db/repositories.py`.
- Created 2 product modules: `operator_step_preview.py` (_assess_risk heuristic by endpoint + method + severity; generates token and persists it), `operator_step_confirmation.py` (get_token_status, confirm_token — auto-expires stale pending tokens on every read/confirm).
- Created `apps/api/schemas/operator_step_token.py` and extended `apps/api/routes/operator_action_plan.py` with 3 new endpoints: POST /v1/operator-console/action-plan/step-preview, GET /v1/operator-console/action-plan/step-preview/{token_id}, POST /v1/operator-console/action-plan/step-confirm.
- Extended `/demo` with "Action Plan Step Preview & Confirmation Tokens" card (Sprint 43) with 3 buttons: tokenPreview (72), tokenStatus (73), tokenConfirm (74). Output in `id="tokenOutput"`.
- Created 4 test files: `test_operator_step_preview.py` (12 tests), `test_operator_step_confirmation.py` (10 tests), `test_operator_step_token_api.py` (13 tests), `test_operator_step_token_ui.py` (11 tests) — 45 new tests, all passing.
- Hard invariants enforced: token confirmation does not execute anything, all responses preserve will_execute=False, can_execute=False, is_advisory_only=True. No ERP writes, no skill mutation, no LLM calls.

- Started Sprint 44 Action Plan Step Registry & Dispatch Eligibility from GitHub issue #37. Adds the safety gate between Sprint 43 confirmation tokens and any future operator-triggered dispatch. Every action plan step must normalize to a known action_key, be allowlist-validated, pass safety policy, and have a confirmed token when required before eligibility is granted. Nothing is executed — the result is an eligibility declaration only.
- Created `operator_action_registry.py`: 9 allowlisted action keys (check_governance_gaps, record_human_decision, create_activation_request, evaluate_final_gate, activate_candidate, run_preview, search_skills, reuse_suggestions, inspect_lifecycle) with safety_tier (read_only / creates_record / governance_required / activation_only), requires_confirmed_token flag, endpoint_template, and a normalization heuristic that maps endpoint_hint strings → action_keys.
- Created `operator_action_dispatch_policy.py`: policy evaluator per safety tier — read_only always eligible; creates_record/governance_required/activation_only require confirmed token.
- Created `operator_action_dispatch_eligibility.py`: orchestrator — normalize → registry lookup → token status check → policy evaluation → DispatchEligibilityResult. Read-only, no mutations.
- Created `operator_action_dispatch_audit.py`: persist + retrieve eligibility events (event prefix `adel_`).
- Added `ActionDispatchEligibilityEvent` DB model to `erpguard/db/models.py` and 2 repository functions to `erpguard/db/repositories.py`.
- Created `apps/api/schemas/operator_action_dispatch.py` and `apps/api/routes/operator_action_dispatch.py` with 3 endpoints: GET /v1/operator-console/action-registry, POST /v1/operator-console/action-plan/dispatch-eligibility, GET /v1/operator-console/action-plan/dispatch-audit. Registered in `apps/api/main.py`.
- Extended `/demo` with "Action Dispatch Eligibility Registry" card (Sprint 44), buttons 75–77. Output in `id="dispatchOutput"`.
- Created 6 test files: `test_operator_action_registry.py` (14 tests), `test_operator_action_dispatch_policy.py` (10 tests), `test_operator_action_dispatch_eligibility.py` (10 tests), `test_operator_action_dispatch_audit.py` (5 tests), `test_operator_action_dispatch_api.py` (16 tests), `test_operator_action_dispatch_ui.py` (11 tests) — 66 new tests, all passing.
- Hard invariants enforced: no arbitrary endpoint execution, only registry-defined action_keys pass validation, confirmed token required for mutating actions, will_execute=False, can_execute=False, is_advisory_only=True on every response. No ERP writes, no skill mutation, no LLM calls, no automatic API call chaining.
- Started Sprint 45 Confirmed Whitelisted Read-Only Action Dispatcher from GitHub issue #38. Adds the first real operator dispatch flow, but only for explicitly allowlisted internal read-only handlers. The server now requires a confirmed token even for Sprint 45 dispatch, re-checks eligibility immediately before dispatch, selects the handler by `action_key`, persists the dispatch result, and records execution audit evidence. `endpoint_hint` remains informational only and is never executed as HTTP.
- Added `recommend_next_step` to `operator_action_registry.py` and kept Sprint 45 dispatchable actions limited to `check_governance_gaps`, `search_skills`, `reuse_suggestions`, `inspect_lifecycle`, and `recommend_next_step`.
- Added 2 new DB models to `erpguard/db/models.py`: `ActionDispatchResultRecord` and `ActionDispatchExecutionAuditEvent`. Added 4 repository functions to `erpguard/db/repositories.py`: `create_action_dispatch_result_record`, `get_action_dispatch_result_record`, `create_action_dispatch_execution_audit_event`, and `list_recent_action_dispatch_execution_audit_events`.
- Created 4 product modules: `operator_action_dispatch_handlers.py` (5 explicit internal read-only handlers over the existing advisory services), `operator_action_dispatch_result.py` (persist + retrieve dispatch results), `operator_action_dispatch_execution_audit.py` (persist + retrieve execution audit events), and `operator_action_dispatcher.py` (confirmed-token gate → Sprint 45 allowlist check → eligibility re-check → internal handler dispatch → result + audit persistence).
- Created `apps/api/schemas/operator_action_dispatch_execute.py` and `apps/api/routes/operator_action_dispatch_execute.py` with 4 endpoints: POST `/v1/operator-console/action-plan/dispatch`, GET `/v1/operator-console/action-plan/dispatch-results/{dispatch_id}`, GET `/v1/operator-console/action-plan/dispatch-execution-audit`, and GET `/v1/operator-console/action-plan/dispatchable-actions`. Registered the router in `apps/api/main.py`.
- Extended `/demo` with "Confirmed Read-Only Action Dispatcher" card (Sprint 45), buttons 78–81, JSON parameters input, result lookup, dispatchable-actions lookup, and execution-audit lookup. Added the visible Sprint 45 safety banner stating that only internal read-only handlers are dispatched.
- Added 6 Sprint 45 test files: `test_operator_action_dispatch_handlers.py`, `test_operator_action_dispatcher.py`, `test_operator_action_dispatch_result.py`, `test_operator_action_dispatch_execution_audit.py`, `test_operator_action_dispatch_execute_api.py`, and `test_operator_action_dispatch_execute_ui.py`. Also extended `test_demo_dashboard.py` to assert the new card is present.
- Updated `README.md` with a `Confirmed Read-Only Action Dispatcher` section documenting the new endpoints, the Sprint 45 allowlist, and the explicit non-goals: no endpoint execution, no ERP writes, no browser/MCP/LLM runtime, no mutation, and no automatic chaining.
- Verification status: implementation and tests were prepared, but test execution is currently blocked in this shell because both `python.exe` and `py.exe` fail to launch with `Una sesión de inicio especificada no existe`. Full pytest and `git diff --check` still need to be run once a working Python interpreter is available in the environment.
- Ran `git diff --check`; result: clean for diff content, with only line-ending warnings (`LF will be replaced by CRLF`) from Git on touched files in this Windows worktree.
- Adjusted Sprint 45 dispatch-result semantics after remote review: `DispatchResultResponse` and `OperatorActionDispatchResult` now use `is_advisory_only=false` because the endpoint performs an internal read-only handler dispatch. `will_execute=false` and `can_execute=false` remain to indicate the response itself is not a pending execution plan.
- Added Sprint 50 Manual Fake ERP Regression Suite on 2026-05-28. New modules: `erpguard/product/fake_erp_regression_case.py`, `erpguard/product/fake_erp_regression_run.py`, `erpguard/product/fake_erp_regression_audit.py`, `apps/api/schemas/fake_erp_regression.py`, `apps/api/routes/fake_erp_regression.py`, plus new regression tables and repository helpers in `erpguard/db/models.py` and `erpguard/db/repositories.py`.
- Sprint 50 purpose: define a deterministic regression case, explicitly run it through Sprint 48 controlled Fake ERP execution, generate or reuse Sprint 49 evidence packs, compare expected vs actual, and persist regression audit evidence. Manual only. No scheduler, no autonomous loop.
- Sprint 50 files also updated: `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, `AGENTS.md`, and 5 new tests: `tests/test_fake_erp_regression_case.py`, `tests/test_fake_erp_regression_run.py`, `tests/test_fake_erp_regression_audit.py`, `tests/test_fake_erp_regression_api.py`, `tests/test_fake_erp_regression_ui.py`.
- Sprint 50 invariants kept: no Odoo, no real ERP, no browser automation, no MCP, no LLM runtime, no external HTTP, no `endpoint_hint` execution, no new execution surface beyond the existing Sprint 48 path.
- Sprint 50 verification used `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe` because `python.exe` in this shell still fails with `Una sesión de inicio especificada no existe. Es posible que haya finalizado`. Passed slices: `tests/test_fake_erp_regression_case.py`, `tests/test_fake_erp_regression_run.py`, `tests/test_fake_erp_regression_audit.py`, `tests/test_fake_erp_regression_api.py`, `tests/test_fake_erp_regression_ui.py`, `tests/test_fake_erp_execution_api.py`, `tests/test_fake_erp_evidence_pack_api.py`, `tests/test_manual_dry_run_api.py`, and `tests/test_operator_action_governance_mutation_dispatcher.py`.
- Added Sprint 51 Universal ERP Adapter Contract on 2026-05-28. New file: `erpguard/product/erp_adapter_contract.py`, plus new test file `tests/test_erp_adapter_contract.py`. Updated `README.md` and `AGENTS.md`.
- Sprint 51 business rationale: ERPGuard core must remain ERP-agnostic. Odoo, SAP, Dynamics, Holded, NetSuite, and custom REST are future adapters behind a neutral contract, not core assumptions.
- Sprint 51 files changed: `erpguard/product/erp_adapter_contract.py`, `tests/test_erp_adapter_contract.py`, `README.md`, `AGENTS.md`.
- Sprint 51 invariants kept: no Sprint 52/53, no registry, no policy API, no Odoo/SAP/Dynamics/Holded/NetSuite/custom REST real implementation, no external HTTP, no credentials, no browser automation, no MCP, no scheduler, no real ERP writes, and no Fake ERP runtime changes.
- Sprint 51 verification used `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe` because `python.exe` in this shell still fails with `Una sesión de inicio especificada no existe. Es posible que haya finalizado`. Passed slices: `tests/test_erp_adapter_contract.py`, `tests/test_fake_erp_regression_run.py`, `tests/test_fake_erp_execution_api.py`, and `tests/test_fake_erp_evidence_pack_api.py`.
- Added Sprint 52 ERP Adapter Capability Registry on 2026-05-28. New files: `erpguard/product/erp_adapter_capability_registry.py`, `apps/api/schemas/erp_adapter_capability.py`, `apps/api/routes/erp_adapter_capability.py`, `tests/test_erp_adapter_capability_registry.py`, `tests/test_erp_adapter_capability_api.py`, `tests/test_erp_adapter_capability_ui.py`. Updated `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Sprint 52 business rationale: ERPGuard must reason about any ERP through declared capabilities before any adapter connects to a real system. This capability model keeps the core ERP-agnostic and lets future adapters plug into a common contract.
- Sprint 52 invariants kept: no Sprint 53, no policy engine, no Odoo/SAP/Dynamics/Holded/NetSuite/custom REST real implementation, no external HTTP, no real credentials, no browser automation, no MCP, no scheduler, no real ERP writes, and no Fake ERP runtime changes.
- Sprint 52 behavior remains advisory-only: `is_advisory_only=true`, `will_execute=false`, `can_execute=false`, and all external execution safety flags remain false. Requested fields are checked only against declared allowlists, not actor/token/policy/readiness logic.
- Sprint 52 verification used `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe`. Passed slices: `tests/test_erp_adapter_capability_registry.py`, `tests/test_erp_adapter_capability_api.py`, `tests/test_erp_adapter_capability_ui.py`, `tests/test_erp_adapter_contract.py`, `tests/test_fake_erp_regression_run.py`, `tests/test_fake_erp_execution_api.py`, and `tests/test_fake_erp_evidence_pack_api.py`.
- Added Sprint 53 ERP Adapter Safety Policy on 2026-05-28. New files: `erpguard/product/erp_adapter_safety_policy.py`, `apps/api/schemas/erp_adapter_policy.py`, `apps/api/routes/erp_adapter_policy.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_policy_api.py`, `tests/test_erp_adapter_contract_matrix.py`, `tests/test_erp_adapter_policy_ui.py`. Updated `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Sprint 53 business rationale: capability declarations alone are not enough; ERPGuard also needs a neutral policy decision layer that says whether a declared capability is eligible, previewable, blocked, or still non-executable in the current product phase.
- Sprint 53 invariants kept: no Connector Autopilot, no real Odoo/SAP/Dynamics/Holded/NetSuite/custom REST implementation, no external HTTP, no real credentials, no browser automation, no MCP, no scheduler, no real ERP writes, and no Fake ERP runtime changes.
- Sprint 53 behavior remains advisory-only: `is_advisory_only=true`, `will_execute=false`, `can_execute=false`, `real_erp_touched=false`, `real_erp_write_performed=false`, and `credentials_used=false` for every result.
- Sprint 53 verification used `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe`. Passed slices: `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_policy_api.py`, `tests/test_erp_adapter_contract_matrix.py`, `tests/test_erp_adapter_policy_ui.py`, `tests/test_erp_adapter_capability_registry.py`, `tests/test_erp_adapter_capability_api.py`, `tests/test_erp_adapter_contract.py`, and `tests/test_fake_erp_regression_run.py`.

- Added Sprint 54 Connector Setup Session for Block D / Connector Autopilot on 2026-05-28. New files: `erpguard/product/connector_setup_session.py`, `erpguard/product/connector_setup_audit.py`, `apps/api/schemas/connector_setup.py`, `apps/api/routes/connector_setup.py`, plus new tests `tests/test_connector_setup_session.py`, `tests/test_connector_setup_api.py`, `tests/test_connector_setup_audit.py`, and `tests/test_connector_setup_ui.py`.
- Sprint 54 changed persistence in `erpguard/db/models.py` and `erpguard/db/repositories.py` with `ConnectorSetupSession` and `ConnectorSetupAuditEvent`, registered the router in `apps/api/main.py`, added the `/demo` Connector Autopilot setup panel, and documented the sprint in `README.md`.
- Sprint 54 business rationale: represent the customer's onboarding intent, "I want to connect this ERP using URL + credentials," while only creating a safe setup-session shell. Future sprints may use a credential vault and fingerprinting; Sprint 54 does not.
- Sprint 54 behavior: HTTPS ERP URLs are accepted and normalized to `erp_url_host`; invalid URLs block with `invalid_erp_url`; external `http://` blocks with `unsafe_url_scheme`; localhost HTTP is allowed for tests only; missing `credential_ref` yields `status=awaiting_credentials` and `credential_mode=not_provided`; opaque `credential_ref` yields `status=ready_for_fingerprint` and `credential_mode=vault_reference_only` but still performs no fingerprinting.
- Sprint 54 audit: setup-session creation records `setup_session_requested` and either `setup_session_created` or `setup_session_blocked`; audit detail stores only redacted/safe metadata and never raw secret values.
- Sprint 54 invariants kept: no Sprint 55, no credential vault implementation, no raw credential storage, no password/API key/token/secret persistence, no external HTTP, no login, no fingerprinting, no schema inspection, no capability generation, no connector activation, no Odoo real adapter, no real ERP adapter, no browser automation, no MCP, no scheduler, and no ERP writes.
- Sprint 54 verification used `C:\Users\jairo\AppData\Local\Python\pythoncore-3.14-64\python.exe` because `python.exe` failed with `Una sesion de inicio especificada no existe`. Passed slices: `tests/test_connector_setup_session.py`, `tests/test_connector_setup_api.py`, `tests/test_connector_setup_audit.py`, `tests/test_connector_setup_ui.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_capability_registry.py`, and `tests/test_erp_adapter_contract.py`. The combined Sprint 54 + Block C command passed with 65 tests.
- Full `python -m pytest -q` could not be completed in this shell: the normal `python.exe` failed to launch, the alternate interpreter first hit a Temp permission error at `C:\Users\jairo\AppData\Local\Temp\pytest-of-jairo`, and reruns with workspace-local `--basetemp` progressed without failures to around 73% but timed out after 15 and then 30 minutes. This was recorded as an environment/runtime timeout, not as a Sprint 54 assertion failure.

### 2026-05-29 — Sprint 56 ERP Fingerprinting Plan (agent)

- What changed: added ERP Fingerprinting Plan (plan-only) for Connector Autopilot Block D. New files: `erpguard/product/erp_fingerprinting_plan.py`, `erpguard/product/erp_fingerprinting_audit.py`, `erpguard/db/models.py` (ERPFingerprintingPlan, ERPFingerprintingAuditEvent), `erpguard/db/repositories.py` (create/get/list plan functions, create/list audit), `apps/api/schemas/erp_fingerprinting.py`, `apps/api/routes/erp_fingerprinting.py`, demo dashboard Sprint 56 card with 4 buttons, and 4 test files.
- Why: Sprint 56 produces a plan-only fingerprinting artifact from setup_session + credential_ref using static heuristics (host/name/manual_hint patterns), without performing any real discovery, network calls, login, schema inspection, capability generation, or connector activation.
- Sprint 56 behavior: setup session + sealed credential_ref → fingerprinting plan → adapter candidates (odoo/holded/sap/dynamics/netsuite/custom_rest/unknown) → planned checks (all plan_only, would_require_network, would_use_credentials) → risk summary → next_safe_step. Blocks on: setup_session_not_found, setup_session_blocked, credential_ref_required, credential_ref_mismatch, credential_not_found, credential_revoked.
- Sprint 56 invariants kept: will_connect=false, external_http_performed=false, login_attempted=false, fingerprint_performed=false, schema_inspection_performed=false, capability_generation_performed=false, read_only_activation_performed=false, credentials_exposed=false, raw_secret_accessed=false. No real discovery, no ERP connection, no HTTP, no login, no fingerprinting, no schema inspection, no capability generation, no connector activation, no browser automation, no MCP, no scheduler, no ERP writes.
- Verification: `python -m pytest tests/test_erp_fingerprinting_plan.py tests/test_erp_fingerprinting_api.py tests/test_erp_fingerprinting_audit.py tests/test_erp_fingerprinting_ui.py tests/test_connector_setup_session.py tests/test_connector_setup_api.py tests/test_connector_credential_vault.py tests/test_connector_credential_api.py tests/test_connector_credential_ui.py tests/test_erp_adapter_safety_policy.py tests/test_erp_adapter_contract.py -v`; result: 108 passed.
- `git diff --check`; result: clean for diff content.
- Explicitly did not add Sprint 57, real discovery, HTTP requests, ERP login, schema inspection, capability generation, connector activation, browser automation, MCP, scheduler, or new ERP write categories.

### 2026-05-29 - Sprint 57 Safe Discovery Plan & Read-Only Surface Model (agent)

- What changed: added Sprint 57 Safe Discovery Plan for Connector Autopilot Block D. New files: `erpguard/product/safe_discovery_plan.py`, `erpguard/product/safe_discovery_audit.py`, `apps/api/schemas/safe_discovery.py`, `apps/api/routes/safe_discovery.py`, and tests `tests/test_safe_discovery_plan.py`, `tests/test_safe_discovery_api.py`, `tests/test_safe_discovery_audit.py`, `tests/test_safe_discovery_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Related full-suite cleanup: updated `tests/test_operator_action_dispatch_handlers.py::test_dispatchable_actions_allowlist` to include the already-registered read-only `run_preview` action, matching existing registry/API/governance tests.
- Why: Sprint 57 turns a Sprint 56 fingerprinting plan into a safe discovery plan plus static read-only surface model, permission surface, planned discovery steps, and blocked write surface without performing real discovery.
- Sprint 57 behavior: fingerprinting_plan -> safe_discovery_plan -> read_only_surface_model -> blocked_write_surface_model. If `selected_adapter_type` is omitted, the highest-confidence fingerprinting candidate is used. Static adapter templates cover Odoo, Holded, SAP, Dynamics, NetSuite, custom_rest, and unknown. Blocks on: fingerprinting_plan_not_found, fingerprinting_plan_not_ready, adapter_candidate_required, credential_ref_required, credential_not_found, credential_revoked.
- Sprint 57 invariants kept: will_connect=false, external_http_performed=false, login_attempted=false, schema_inspection_performed=false, permission_inspection_performed=false, sample_data_read=false, capability_generation_performed=false, read_only_activation_performed=false, erp_write_performed=false, credentials_exposed=false, raw_secret_accessed=false. All write-like operations are blocked in the model.
- Verification: requested Sprint 57 and regression slices passed: `tests/test_safe_discovery_plan.py`, `tests/test_safe_discovery_api.py`, `tests/test_safe_discovery_audit.py`, `tests/test_safe_discovery_ui.py`, `tests/test_erp_fingerprinting_plan.py`, `tests/test_erp_fingerprinting_api.py`, `tests/test_connector_credential_vault.py`, `tests/test_connector_credential_api.py`, `tests/test_connector_setup_session.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_capability_registry.py`, and `tests/test_erp_adapter_contract.py`. Full `python -m pytest -q` passed after the allowlist cleanup, with only existing warning output.
- `git diff --check`; result: clean for diff content.
- Explicitly did not add Sprint 58, real discovery, HTTP requests, ERP login, schema inspection, permission inspection, ERP data reads, capability generation, connector activation, Odoo real implementation, real ERP adapters, browser automation, MCP, scheduler, ERP writes, or raw secret access.

### 2026-05-29 - Sprint 58 Auto Capability Generation from Discovery (agent)

- What changed: added generated advisory capability sets for Connector Autopilot Block D. New files: `erpguard/product/generated_capability_set.py`, `erpguard/product/generated_capability_audit.py`, `apps/api/schemas/generated_capabilities.py`, `apps/api/routes/generated_capabilities.py`, and tests `tests/test_generated_capability_set.py`, `tests/test_generated_capability_api.py`, `tests/test_generated_capability_audit.py`, `tests/test_generated_capability_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Related full-suite cleanup: updated `apps/api/routes/operator_action_plan.py` and `apps/api/routes/operator_action_dispatch.py` so API audit `event_count` reports the total persisted audit count instead of the limited page size. The underlying action-plan audit service still reports page size for its existing unit contract.
- Why: Sprint 58 converts a Sprint 57 safe discovery plan into a generated capability set, capability registry overlay data, advisory read-only capabilities, and blocked write capabilities without activating or executing anything.
- Sprint 58 behavior: safe_discovery_plan -> generated_capability_set -> capability_registry_overlay -> policy-checkable advisory capabilities. Generated operations include `read_object`, `search_objects`, `inspect_schema`, and `inspect_permissions`; blocked capabilities mirror the Sprint 57 blocked write surface. Blocks on: safe_discovery_plan_not_found, safe_discovery_plan_not_ready, credential_ref_required, credential_not_found, credential_revoked, read_only_surface_required.
- Sprint 58 invariants kept: is_advisory_only=true, will_execute=false, can_execute=false, supports_execution=false, external_http_performed=false, login_attempted=false, schema_inspection_performed=false, permission_inspection_performed=false, sample_data_read=false, read_only_activation_performed=false, erp_write_performed=false, credentials_exposed=false, raw_secret_accessed=false. `capability_generation_performed=true` only represents internal artifact generation.
- Verification: requested Sprint 58 and regression commands passed: `tests/test_generated_capability_set.py`, `tests/test_generated_capability_api.py`, `tests/test_generated_capability_audit.py`, `tests/test_generated_capability_ui.py`, `tests/test_safe_discovery_plan.py`, `tests/test_safe_discovery_api.py`, `tests/test_erp_fingerprinting_plan.py`, `tests/test_erp_fingerprinting_api.py`, `tests/test_connector_credential_vault.py`, `tests/test_connector_setup_session.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_capability_registry.py`, and `tests/test_erp_adapter_contract.py`. Full `python -m pytest -q` passed with only existing warning output.
- Explicitly did not add Sprint 59, connector activation, external HTTP, ERP URL calls, real login, real schema inspection, real permission inspection, ERP data reads, Odoo real implementation, real ERP adapters, browser automation, MCP, scheduler, ERP writes, or raw secret access.

### 2026-05-29 - Sprint 59 Read-Only Connector Activation (agent)

- What changed: added Read-Only Connector Activation to close Block D. New files: `erpguard/product/read_only_connector_activation.py`, `erpguard/product/read_only_connector_activation_audit.py`, `apps/api/schemas/read_only_connector_activation.py`, `apps/api/routes/read_only_connector_activation.py`, and tests `tests/test_read_only_connector_activation.py`, `tests/test_read_only_connector_activation_api.py`, `tests/test_read_only_connector_activation_audit.py`, `tests/test_read_only_connector_activation_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Why: Sprint 59 creates a governed internal `active_read_only` connector artifact from the full Connector Autopilot chain, but only after explicit human approval. It does not open any real ERP connection.
- Sprint 59 behavior: generated capability set -> activation request (`requested`, `requires_human_approval=true`) -> explicit approval -> activation artifact (`active_read_only`). Blocks on: capability_set_not_found, capability_set_not_ready, credential_not_found, credential_revoked, generated_capabilities_required, executable_write_capability_detected, mode_not_allowed, activation_request_not_found, activation_request_not_ready, approval_actor_required.
- Sprint 59 invariants kept: external_http_performed=false, login_attempted=false, real_erp_connection_established=false, real_erp_read_enabled=false, real_erp_write_enabled=false, browser_control_performed=false, mcp_execution_performed=false, scheduler_used=false, credentials_exposed=false, raw_secret_accessed=false.
- Block D closure note: the artifact chain now exists from setup session through credential vault, fingerprinting plan, safe discovery plan, generated capabilities, human approval, and internal read-only activation. `active_read_only` means approved internal artifact only, not live ERP connectivity.
- Verification: requested Sprint 59 and regression commands passed: `tests/test_read_only_connector_activation.py`, `tests/test_read_only_connector_activation_api.py`, `tests/test_read_only_connector_activation_audit.py`, `tests/test_read_only_connector_activation_ui.py`, `tests/test_generated_capability_set.py`, `tests/test_generated_capability_api.py`, `tests/test_safe_discovery_plan.py`, `tests/test_safe_discovery_api.py`, `tests/test_erp_fingerprinting_plan.py`, `tests/test_connector_credential_vault.py`, `tests/test_connector_setup_session.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_capability_registry.py`, and `tests/test_erp_adapter_contract.py`. Full `python -m pytest -q` passed with only existing warning output.
- Explicitly did not add Block E, Odoo real implementation, real ERP adapters, external HTTP, ERP URL calls, real login, real schema inspection, real permission inspection, ERP data reads, browser automation, MCP, scheduler, ERP writes, or raw secret access.

### 2026-05-29 - Sprint 74 Odoo Read-Only Adapter Shell (agent)

- What changed: added the Odoo Read-Only Adapter Shell as the first Block G artifact. New files: `erpguard/product/odoo_read_only_adapter.py`, `erpguard/product/odoo_read_only_adapter_audit.py`, `apps/api/schemas/odoo_read_only_adapter.py`, `apps/api/routes/odoo_read_only_adapter.py`, and tests `tests/test_odoo_read_only_adapter_shell.py`, `tests/test_odoo_read_only_adapter_api.py`, `tests/test_odoo_read_only_adapter_audit.py`, `tests/test_odoo_read_only_adapter_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Why: Block D produced an approved internal `active_read_only` connector artifact. Sprint 74 defines the next boundary: an Odoo adapter shell that can be connected later by a controlled future sprint, without connecting now.
- Sprint 74 behavior: active_read_only activation -> OdooReadOnlyAdapter shell -> future controlled connection test. The shell validates activation existence, `active_read_only` status, `adapter_type=odoo`, `mode=read_only`, credential reference presence, and non-revoked credential metadata. Blocks on: activation_not_found, activation_not_active_read_only, adapter_type_not_odoo, mode_not_read_only, credential_ref_required, credential_not_found, credential_revoked.
- Sprint 74 invariants kept: can_connect_later=true only for prepared shell, will_connect_now=false, external_http_performed=false, login_attempted=false, odoo_rpc_performed=false, odoo_read_performed=false, odoo_write_performed=false, schema_inspection_performed=false, permission_inspection_performed=false, credentials_exposed=false, raw_secret_accessed=false.
- Verification: requested Sprint 74 and regression commands passed: `tests/test_odoo_read_only_adapter_shell.py`, `tests/test_odoo_read_only_adapter_api.py`, `tests/test_odoo_read_only_adapter_audit.py`, `tests/test_odoo_read_only_adapter_ui.py`, `tests/test_read_only_connector_activation.py`, `tests/test_generated_capability_set.py`, `tests/test_safe_discovery_plan.py`, `tests/test_erp_fingerprinting_plan.py`, `tests/test_connector_credential_vault.py`, `tests/test_connector_setup_session.py`, `tests/test_erp_adapter_safety_policy.py`, `tests/test_erp_adapter_capability_registry.py`, and `tests/test_erp_adapter_contract.py`. Full `python -m pytest -q` passed with only existing warning output.
- Explicitly did not add Sprint 75, Odoo connection, HTTP, XML-RPC, JSON-RPC, login, Odoo reads, Odoo writes, real schema inspection, real permission inspection, browser automation, MCP, scheduler, or raw secret access.

### 2026-05-30 - Sprint 75 Odoo Connection Test via active_read_only Connector (agent)

- What changed: added the controlled Odoo connection/auth test artifact after the Sprint 74 adapter shell. New files: `erpguard/product/odoo_connection_test.py`, `erpguard/product/odoo_connection_test_audit.py`, `apps/api/schemas/odoo_connection_test.py`, `apps/api/routes/odoo_connection_test.py`, and tests `tests/test_odoo_connection_test.py`, `tests/test_odoo_connection_test_api.py`, `tests/test_odoo_connection_test_audit.py`, `tests/test_odoo_connection_test_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Why: Block G needs a separate boundary between a prepared Odoo adapter shell and a future live adapter. Sprint 75 adds an explicit `allow_network_test` gate and records the connection-test result without business reads or writes.
- Sprint 75 behavior: prepared `odoo_ro_...` adapter session -> `odoo_ct_...` connection test. By default `allow_network_test=false` blocks with `network_test_not_allowed` and performs no network/login/RPC. When explicitly allowed, the service delegates only to a mockable connection/auth client and records passed/failed status while keeping business-data-read, schema inspection, permission inspection, and write flags false.
- Invariants kept: no partners, products, sale orders, invoices, stock, MRP, business records, business schemas, real permissions, create/update/delete/write/unlink, order confirmation, invoice posting, stock movement, manufacturing completion, browser automation, MCP, scheduler, raw secret exposure, prompt credential exposure, or LLM credential access.
- Verification: Sprint 75 targeted tests and requested Block G/Block D/Block C regression slices were executed during this session; final results are recorded by the agent response.
- Explicitly did not add Sprint 76.

### 2026-05-30 - Sprint 76 Odoo Partner/Product/Sale Order Read Mapping (agent)

- What changed: added controlled Odoo read mapping for `partner`, `product`, and `sale_order` after a passed Sprint 75 connection test. New files: `erpguard/product/odoo_read_mapping.py`, `erpguard/product/odoo_read_mapping_audit.py`, `apps/api/schemas/odoo_read_mapping.py`, `apps/api/routes/odoo_read_mapping.py`, and tests `tests/test_odoo_read_mapping.py`, `tests/test_odoo_read_mapping_api.py`, `tests/test_odoo_read_mapping_audit.py`, `tests/test_odoo_read_mapping_ui.py`. Updated `erpguard/db/models.py`, `erpguard/db/repositories.py`, `apps/api/main.py`, `apps/api/routes/demo_dashboard.py`, `README.md`, and `AGENTS.md`.
- Why: Block G needs the first safe business-data read surface, but only after the full Connector Autopilot chain has produced an active read-only artifact, Odoo shell, and passed connection test.
- Sprint 76 behavior: passed `odoo_ct_...` connection test -> `odoo_map_...` read mapping. `allow_business_read=false` blocks by default without calling the read client. With explicit allow, only an injectable/mocked read client may read one allowlisted object and lookup key, then map to canonical ERP objects.
- Invariants kept: no invoice, stock, MRP, journal entry, payment, custom object, arbitrary model, arbitrary field, user domain, write/create/update/delete/unlink, order confirmation, invoice posting, payment reconciliation, stock movement, manufacturing completion, browser automation, MCP, scheduler, raw secret exposure, prompt credential exposure, or LLM credential access.
- Verification: Sprint 76 targeted tests and requested Block G/Block D/Block C regression slices were executed during this session; final results are recorded by the agent response.
- Explicitly did not add Sprint 77.
