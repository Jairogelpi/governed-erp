# Governance

ERPGuard Evolution is maintained through reviewed pull requests. Changes to the safety kernel, public contracts, connector boundaries or phase scope require maintainer review and explicit tests.

The master implementation specification and `AGENTS.md` define the active phase contract. Architectural deviations require an ADR. No contributor may bypass the safety kernel to make a demo pass.

